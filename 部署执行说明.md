# 部署执行说明

## 🚀 快速部署

### 方式1: 直接指定服务器地址（推荐）

**Windows PowerShell**:
```powershell
.\scripts\quick_deploy.ps1 -ServerHost your-server-ip
```

**Linux/Mac**:
```bash
export SERVER_HOST=your-server-ip
./scripts/deploy_all_to_server.sh
```

### 方式2: 交互式部署

**Windows PowerShell**:
```powershell
.\scripts\deploy_interactive.ps1
```

然后按提示输入服务器信息。

### 方式3: 完整参数部署

**Windows PowerShell**:
```powershell
.\scripts\deploy_all_to_server.ps1 -ServerHost your-server-ip -ServerUser root -ServerPath /opt/enterprise-ai-platform
```

**Linux/Mac**:
```bash
export SERVER_HOST=your-server-ip
export SERVER_USER=root
export SERVER_PATH=/opt/enterprise-ai-platform
./scripts/deploy_all_to_server.sh
```

---

## 📋 部署前准备

### 1. 确保服务器可访问

```bash
# 测试SSH连接
ssh root@your-server-ip "echo 'SSH连接成功'"
```

### 2. 确保服务器已安装Docker

```bash
# 在服务器上检查
ssh root@your-server-ip "docker --version && docker-compose --version"
```

### 3. 确保服务器有足够资源

- 内存: >= 16GB
- 磁盘空间: 至少10GB可用
- 端口: 7474, 7687未被占用

---

## 🔍 部署过程说明

部署脚本会自动执行以下步骤：

1. **备份数据** - 备份PostgreSQL数据到本地
2. **同步代码** - 上传所有相关代码文件
3. **安装依赖** - 安装neo4j Python驱动
4. **启动Neo4j** - 启动Neo4j服务
5. **数据迁移** - 迁移数据到Neo4j和Qdrant（可选）
6. **重启服务** - 重启API Gateway和Metadata Service
7. **运行测试** - 验证部署是否成功

---

## ⚠️ 注意事项

1. **首次部署**: 需要提供服务器地址
2. **数据迁移**: 首次部署建议执行数据迁移
3. **服务重启**: 部署后会自动重启相关服务
4. **网络要求**: 确保本地可以SSH连接到服务器

---

## 📝 示例

### 示例1: 部署到192.168.1.100

```powershell
.\scripts\quick_deploy.ps1 -ServerHost 192.168.1.100
```

### 示例2: 部署到域名服务器

```powershell
.\scripts\quick_deploy.ps1 -ServerHost server.example.com -ServerUser admin
```

---

**提示**: 如果不知道服务器地址，请先联系服务器管理员获取。



