# 企业架构改造部署执行指南

**日期**: 2025-12-07  
**状态**: ✅ **测试通过，准备部署到服务器**

---

## ✅ 本地测试完成

### 测试结果
- ✅ 数据库迁移: 100% 通过
- ✅ 模型定义: 100% 通过  
- ✅ 服务层测试: 100% 通过
- ✅ 所有新表创建成功
- ✅ 所有增强字段添加成功

---

## 🚀 部署到服务器

### 步骤1: 配置部署脚本

编辑 `scripts/deploy_enterprise_architecture.sh`，修改以下配置：

```bash
# 应用服务器配置
APP_SERVER_HOST="your-app-server-ip"  # 修改为实际服务器IP或域名
APP_SERVER_USER="root"  # 修改为实际SSH用户名
APP_SERVER_KEY="./remote.ssh"  # 密钥文件（已在根目录，无需修改）

# Neo4j服务器配置（如果Neo4j和应用在同一服务器，设置为相同地址）
NEO4J_SERVER_HOST="your-neo4j-server-ip"  # 修改为实际Neo4j服务器IP
NEO4J_SERVER_USER="root"
NEO4J_SERVER_KEY="./remote.ssh"

# 项目目录（根据实际情况修改）
PROJECT_DIR="/opt/enterprise-ai-platform"
```

### 步骤2: 测试SSH连接

```bash
# 测试应用服务器连接
ssh -i ./remote.ssh user@your-app-server "echo '连接成功'"

# 如果连接失败，检查：
# 1. 服务器地址是否正确
# 2. 用户名是否正确  
# 3. 密钥文件权限（Windows下可能不需要chmod）
```

### 步骤3: 运行部署脚本

**Windows PowerShell**:
```powershell
# 使用Git Bash或WSL运行
bash scripts/deploy_enterprise_architecture.sh

# 或者使用PowerShell直接执行（需要安装OpenSSH）
ssh -i ./remote.ssh user@server "命令"
```

**Linux/Mac**:
```bash
# 设置执行权限
chmod +x scripts/deploy_enterprise_architecture.sh

# 运行部署
bash scripts/deploy_enterprise_architecture.sh
```

---

## 📋 部署脚本执行流程

部署脚本会自动执行以下步骤：

1. ✅ **检查SSH密钥** - 验证密钥文件存在
2. ✅ **备份数据库** - 在服务器上备份数据库
3. ✅ **上传代码** - 使用rsync上传代码到服务器
4. ✅ **运行迁移** - 在服务器上执行数据库迁移
5. ✅ **测试迁移** - 运行测试脚本验证迁移
6. ✅ **重启服务** - 重启相关服务
7. ✅ **检查Neo4j** - 检查Neo4j服务器连接（如果分离部署）
8. ✅ **API测试** - 测试API端点（如果已实现）

---

## ✅ 部署后验证

### 1. 验证数据库迁移

```bash
ssh -i ./remote.ssh user@your-server << 'EOF'
    cd /opt/enterprise-ai-platform/database
    python -m alembic current
    # 应该显示: 026 或合并后的版本号
EOF
```

### 2. 验证新表

```bash
ssh -i ./remote.ssh user@your-server << 'EOF'
    psql -U postgres -d enterprise_ai -c "\dt organization_*"
    psql -U postgres -d enterprise_ai -c "\dt technology_*"
EOF
```

### 3. 验证增强字段

```bash
ssh -i ./remote.ssh user@your-server << 'EOF'
    psql -U postgres -d enterprise_ai -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'business_processes' AND column_name = 'organization_id';"
EOF
```

### 4. 验证服务状态

```bash
ssh -i ./remote.ssh user@your-server << 'EOF'
    docker-compose ps
    # 或
    systemctl status enterprise-ai-platform
EOF
```

---

## 🔧 如果部署脚本无法使用（Windows环境）

如果bash脚本无法在Windows上运行，可以使用以下手动步骤：

### 手动部署步骤

#### 1. 备份数据库

```powershell
ssh -i ./remote.ssh user@your-server "pg_dump -U postgres enterprise_ai > /opt/backups/backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').sql"
```

#### 2. 上传代码

使用WinSCP、FileZilla或其他SFTP工具上传代码，或使用PowerShell：

```powershell
# 需要安装Posh-SSH模块
# Install-Module -Name Posh-SSH
$session = New-SSHSession -ComputerName your-server -Credential (Get-Credential) -KeyFile ./remote.ssh
Set-SCPFile -ComputerName your-server -Credential (Get-Credential) -LocalFile ./ -RemotePath /opt/enterprise-ai-platform/ -Recursive
```

#### 3. 运行迁移

```powershell
ssh -i ./remote.ssh user@your-server "cd /opt/enterprise-ai-platform/database && python -m alembic upgrade 026"
```

#### 4. 验证迁移

```powershell
ssh -i ./remote.ssh user@your-server "cd /opt/enterprise-ai-platform && python scripts/test_enterprise_architecture_migration.py"
```

#### 5. 重启服务

```powershell
ssh -i ./remote.ssh user@your-server "cd /opt/enterprise-ai-platform && docker-compose restart metadata-service api-gateway"
```

---

## 📝 部署检查清单

### 部署前
- [x] 本地测试通过
- [x] 代码文件完整
- [x] 部署脚本准备就绪
- [x] 密钥文件存在 (`remote.ssh`)
- [ ] 服务器地址配置
- [ ] SSH连接测试

### 部署中
- [ ] 数据库备份完成
- [ ] 代码上传完成
- [ ] 数据库迁移执行
- [ ] 迁移验证通过
- [ ] 服务重启完成

### 部署后
- [ ] 数据库迁移验证
- [ ] 新表创建验证
- [ ] 增强字段验证
- [ ] 服务状态验证
- [ ] 日志检查无错误

---

## 🎯 快速部署命令

### 使用部署脚本（推荐）

```bash
# 1. 编辑脚本配置服务器地址
# 2. 运行部署
bash scripts/deploy_enterprise_architecture.sh
```

### 手动部署（如果脚本无法使用）

参考上面的手动部署步骤，或使用您常用的部署工具。

---

## ✅ 部署完成标准

部署完成后，应该满足以下条件：

- [ ] 数据库迁移版本为 026
- [ ] 5个新表存在（organization_units, business_roles, technology_types, technology_instances, organization_business_relationships）
- [ ] 8个增强字段存在
- [ ] 服务正常启动
- [ ] 无错误日志

---

## 📞 需要帮助？

如果部署过程中遇到问题，请检查：

1. **SSH连接**: 确保可以连接到服务器
2. **数据库连接**: 确保服务器上数据库运行正常
3. **文件权限**: 确保脚本有执行权限
4. **环境变量**: 确保服务器上配置了正确的环境变量

---

**准备状态**: ✅ **可以部署**  
**下一步**: 配置服务器地址并执行部署

