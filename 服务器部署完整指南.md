# 服务器部署完整指南

## 📋 部署概述

**目标**: 将所有代码和数据同步到服务器，并启动所有服务

---

## 🚀 快速部署

### Windows PowerShell

```powershell
.\scripts\deploy_all_to_server.ps1 -ServerHost your-server-ip -ServerUser root
```

### Linux/Mac

```bash
export SERVER_HOST=your-server-ip
export SERVER_USER=root
./scripts/deploy_all_to_server.sh
```

---

## 📝 详细部署步骤

### 步骤1: 备份服务器数据

**目的**: 在部署前备份现有数据，防止数据丢失

**操作**:
- 自动备份PostgreSQL数据
- 备份文件保存在本地 `./backups/` 目录

### 步骤2: 同步代码文件

**同步的文件**:
1. `docker-compose.yml` - Docker Compose配置（包含Neo4j服务）
2. `.env` - 环境变量配置
3. `database/src/core/neo4j_client.py` - Neo4j客户端
4. `scripts/migrate_data_to_neo4j.py` - 数据迁移脚本
5. `scripts/migrate_vectors_to_qdrant.py` - 向量迁移脚本
6. `scripts/test_database_migration.py` - 测试脚本
7. `api-gateway/src/routes/neo4j_graph.py` - API Gateway路由
8. `metadata-service/src/api/neo4j_api.py` - Metadata Service API
9. `web-ui/src/services/neo4jGraphService.ts` - 前端服务层
10. `web-ui/src/app/neo4j-graph/page.tsx` - 前端页面

**自动更新**:
- `api-gateway/src/main.py` - 添加neo4j_graph路由
- `metadata-service/src/main.py` - 添加neo4j_api路由

### 步骤3: 安装依赖

**操作**:
- 在metadata-service容器中安装 `neo4j==5.14.0`
- 在api-gateway容器中安装 `neo4j==5.14.0`

### 步骤4: 启动Neo4j服务

**操作**:
- 启动Neo4j容器
- 等待30秒确保服务完全启动
- 检查服务状态和日志

### 步骤5: 运行数据迁移

**操作**:
- 迁移知识图谱数据到Neo4j
- 迁移向量数据到Qdrant
- 显示迁移进度和结果

**注意**: 如果数据已迁移，会跳过或显示警告

### 步骤6: 重启相关服务

**操作**:
- 重启API Gateway（加载新的路由）
- 重启Metadata Service（加载新的API）
- 检查服务状态

### 步骤7: 运行测试

**操作**:
- 运行迁移测试脚本
- 验证数据迁移是否成功
- 检查服务连接是否正常

---

## 🔍 验证部署

### 1. 检查Neo4j服务

```bash
# SSH到服务器
ssh user@server

# 检查Neo4j容器状态
cd /opt/enterprise-ai-platform
docker-compose ps neo4j

# 查看Neo4j日志
docker-compose logs neo4j

# 测试Neo4j连接
docker-compose exec neo4j cypher-shell -u neo4j -p neo4j_password "RETURN 1"
```

### 2. 检查API服务

```bash
# 检查API Gateway
curl http://localhost:8080/api/neo4j/graph?limit=10

# 检查Metadata Service
curl http://localhost:8005/api/neo4j/graph?limit=10
```

### 3. 检查前端页面

- 访问: `http://your-server-ip:3000/neo4j-graph`
- 应该能看到图数据可视化页面

### 4. 检查数据迁移

```bash
# 在服务器上运行测试
docker-compose exec metadata-service python /app/scripts/test_database_migration.py
```

---

## ⚠️ 注意事项

### 1. 服务器要求

- **内存**: >= 16GB（Neo4j需要约2-3GB）
- **磁盘空间**: 确保有足够空间存储Neo4j数据
- **端口**: 确保7474和7687端口未被占用

### 2. 环境变量

确保服务器上的 `.env` 文件包含：

```env
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j_password
```

### 3. 网络配置

- Neo4j容器需要能够访问其他服务
- 确保Docker网络配置正确

### 4. 数据迁移

- 首次迁移可能需要较长时间（取决于数据量）
- 如果数据已迁移，可以跳过迁移步骤
- 迁移过程中不要中断

---

## 🔧 故障排查

### 问题1: Neo4j无法启动

**症状**: 容器启动后立即退出

**排查**:
```bash
# 查看详细日志
docker-compose logs neo4j

# 检查内存
docker stats neo4j

# 检查端口占用
netstat -tulpn | grep 7474
```

**解决**:
- 检查内存是否足够
- 检查端口是否被占用
- 检查配置文件是否正确

### 问题2: 数据迁移失败

**症状**: 迁移脚本报错

**排查**:
```bash
# 检查PostgreSQL连接
docker-compose exec postgres psql -U ai_user -d ai_platform -c "SELECT 1"

# 检查Neo4j连接
docker-compose exec neo4j cypher-shell -u neo4j -p neo4j_password "RETURN 1"
```

**解决**:
- 确保所有服务正常运行
- 检查数据库连接配置
- 查看详细错误日志

### 问题3: API无法访问

**症状**: API返回404或500错误

**排查**:
```bash
# 检查服务是否运行
docker-compose ps api-gateway metadata-service

# 查看服务日志
docker-compose logs api-gateway
docker-compose logs metadata-service
```

**解决**:
- 确保服务已重启
- 检查路由是否正确注册
- 查看错误日志

---

## 📊 部署后检查清单

### 服务状态

- [ ] Neo4j服务正常运行
- [ ] API Gateway服务正常运行
- [ ] Metadata Service服务正常运行
- [ ] 前端服务正常运行

### 数据验证

- [ ] 知识图谱数据已迁移到Neo4j
- [ ] 向量数据已迁移到Qdrant
- [ ] 数据数量匹配

### 功能验证

- [ ] 可以访问Neo4j Browser
- [ ] API接口可以正常访问
- [ ] 前端页面可以正常显示
- [ ] 图数据可以正常查询

---

## 🎯 下一步

部署完成后：

1. **验证功能**
   - 访问前端页面测试功能
   - 测试各种查询场景
   - 检查错误处理

2. **性能优化**
   - 根据实际使用情况调整配置
   - 优化查询性能
   - 添加缓存机制

3. **监控设置**
   - 设置服务监控
   - 设置告警规则
   - 定期检查日志

---

**指南创建时间**: 2025-12-08  
**状态**: ✅ 部署脚本已创建，可以开始部署



