# Workflow Engine 启动说明

## ✅ 已完成的配置

1. **虚拟环境**: `workflow-engine/venv` 已创建
2. **依赖安装**: 所有依赖已安装（包括shared_libs）
3. **路径修复**: shared_libs路径问题已修复
4. **别名模块**: 已创建所有必要的别名模块以保持向后兼容

## 📍 shared_libs 真实路径

- **真实路径**: `E:\enterprise-ai-platform\shared_libs`
- **PYTHONPATH**: 已设置为 `E:\enterprise-ai-platform\shared_libs`
- **别名模块**: 
  - `shared_libs/common/*` → `shared_libs/luminaos_common/common/*`
  - `shared_libs/schemas/*` → `shared_libs/luminaos_common/schemas/*`

## 🚀 启动方式

### 方式1: 使用启动脚本（推荐）

```powershell
cd workflow-engine
.\run-debug.ps1
```

### 方式2: 手动启动

```powershell
cd workflow-engine
.\venv\Scripts\Activate.ps1
$env:PYTHONPATH = "E:\enterprise-ai-platform\shared_libs;$env:PYTHONPATH"
$env:REDIS_HOST = "localhost"
python -m uvicorn src.main:app --host 127.0.0.1 --port 8002 --reload
```

## ⚠️ 当前已知问题

1. **database模块错误**: SQLAlchemy的`metadata`属性名冲突（在`database/src/models/agent_models.py`中）
   - 这不是shared_libs路径问题
   - 需要修复database模块中的模型定义

## 🔍 验证路径

运行以下命令验证路径是否正确：

```powershell
.\venv\Scripts\Activate.ps1
$env:PYTHONPATH = "E:\enterprise-ai-platform\shared_libs;$env:PYTHONPATH"
python -c "from shared_libs.common.logger import setup_logger; print('✅ 路径正确')"
```

## 📝 路径配置说明

在 `workflow-engine/src/main.py` 中，路径配置如下：

```python
# 获取项目根目录（workflow-engine的父目录）
_current_dir = os.path.dirname(os.path.abspath(__file__))
_project_root = os.path.dirname(os.path.dirname(_current_dir))
_shared_libs_path = os.path.join(_project_root, 'shared_libs')

# 添加shared_libs路径（支持Windows和Linux）
if _shared_libs_path not in sys.path:
    sys.path.insert(0, _shared_libs_path)
```

这确保了：
- Windows: `E:\enterprise-ai-platform\shared_libs`
- Linux/Docker: `/app/../shared_libs` 或 `/shared_libs`

