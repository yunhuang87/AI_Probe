# 手动查看知识库服务日志

由于SSH连接不稳定，请在新终端窗口手动执行以下命令：

## 方法1：查看最近日志（推荐）

```bash
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker logs enterprise-ai-knowledge-base --tail 200"
```

## 方法2：查看错误日志

```bash
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker logs enterprise-ai-knowledge-base --tail 500 2>&1 | grep -i 'error\|exception\|failed\|parsing\|timeout'"
```

## 方法3：实时查看日志

```bash
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker logs enterprise-ai-knowledge-base -f"
```

## 方法4：查看特定时间段的日志

```bash
# 查看最近10分钟的日志
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker logs enterprise-ai-knowledge-base --since 10m"
```

## 方法5：查看容器状态和资源使用

```bash
# 查看容器状态
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker ps | grep knowledge"

# 查看容器资源使用
ssh -i enterprise_ai_platform.pem -o StrictHostKeyChecking=no ubuntu@43.143.139.197 "docker stats enterprise-ai-knowledge-base --no-stream"
```

## 需要关注的关键信息

1. **解析阶段日志**：查找包含 "parsing"、"extract"、"process_document" 的日志
2. **错误信息**：查找 "Error"、"Exception"、"Failed" 等关键词
3. **超时信息**：查找 "timeout"、"hang"、"stuck" 等
4. **文档ID**：查找正在处理的文档ID，看是否卡在某个特定文档

## 常见问题

- **如果看到 "processing" 状态但长时间无进展**：可能是文档太大或格式有问题
- **如果看到错误但被重试**：查看重试次数和最终结果
- **如果没有任何日志**：可能是后台任务没有启动或日志级别设置问题

