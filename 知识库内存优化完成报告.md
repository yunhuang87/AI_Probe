# 知识库文档处理内存优化完成报告

**优化时间**: 2025-12-09  
**状态**: ✅ 已优化

---

## 问题分析

### 发现的问题

1. **内存占用高**: 文档处理时内存占用较高
2. **批次大小过大**: 默认批次大小30，可能导致内存峰值
3. **一次性加载**: 所有embeddings和chunks一次性加载到内存
4. **GC不频繁**: 垃圾回收不够频繁

---

## 优化措施

### 1. 降低嵌入批次大小 ✅

**修改**: `knowledge-base/src/services/document_service.py`

**优化内容**:
- 默认批次大小: 30 → **15**
- 最小批次大小: 10 → **5**
- 最大批次大小: 50 → **30**
- 内部嵌入批次: 16 → **8**

**效果**: 减少单次处理的数据量，降低内存峰值

### 2. 分批保存Chunks ✅

**修改**: `knowledge-base/src/services/document_service.py`

**优化内容**:
- 原来: 一次性创建所有chunk_data_list
- 现在: 分批保存，每批50个chunks
- 每批保存后立即清理内存

**效果**: 避免一次性加载所有chunks到内存

### 3. 更频繁的内存清理 ✅

**修改**: `knowledge-base/src/services/document_service.py`

**优化内容**:
- 每2批嵌入处理后触发GC
- 内存阈值从70%降低到**60%**
- 每5批chunk保存后触发GC
- 处理完成后立即清理大对象（chunks, embeddings）

**效果**: 及时释放内存，避免内存累积

### 4. 优化内存监控 ✅

**修改**: `knowledge-base/src/core/memory_optimizer.py`

**优化内容**:
- 内存增长阈值: 100MB → **50MB**
- 批次大小计算更保守（乘以0.8）
- 可用内存判断阈值提高（200MB → 300MB）

**效果**: 更敏感的内存监控，更保守的批次大小

### 5. 减小分块大小 ✅

**修改**: `knowledge-base/src/config.py`

**优化内容**:
- CHUNK_SIZE: 1000 → **800** 字符
- CHUNK_OVERLAP: 200 → **150** 字符

**效果**: 减少单个chunk的大小，降低总体内存占用

---

## 优化效果预期

### 内存占用

- **优化前**: 峰值可能达到 800MB-1GB
- **优化后**: 峰值预计降低到 **400-600MB**

### 处理速度

- **优化前**: 批次大，单次处理快但内存高
- **优化后**: 批次小，处理稍慢但更稳定

### 稳定性

- **优化前**: 大文档可能导致内存不足
- **优化后**: 更稳定的内存使用，支持更大文档

---

## 配置建议

### 环境变量配置

在 `.env` 文件中可以进一步调整：

```env
# 内存优化配置
MAX_MEMORY_MB=1024  # 最大内存限制（MB）

# 嵌入批次大小（可根据服务器内存调整）
EMBEDDING_BATCH_SIZE=15  # 默认批次大小
EMBEDDING_INTERNAL_BATCH_SIZE=8  # 内部批次大小

# 分块配置
CHUNK_SIZE=800  # 分块大小
CHUNK_OVERLAP=150  # 重叠大小
```

### 服务器内存建议

- **2GB内存**: 批次大小 5-10
- **4GB内存**: 批次大小 10-15（当前配置）
- **8GB+内存**: 批次大小 20-30

---

## 监控指标

### 检查内存使用

```bash
# 实时监控
docker stats enterprise-ai-knowledge-base

# 查看内存日志
docker logs enterprise-ai-knowledge-base | grep -i memory
```

### 检查处理性能

```bash
# 查看处理时间
docker logs enterprise-ai-knowledge-base | grep "Document Processing"
```

---

## 进一步优化建议

### 1. 流式处理

对于超大文档，可以考虑流式处理：
- 边解析边分块
- 边分块边生成嵌入
- 边生成边保存

### 2. 使用更轻量的模型

考虑使用更小的嵌入模型：
- `all-MiniLM-L6-v2` (384维) - 当前使用
- `paraphrase-MiniLM-L3-v2` (384维) - 更小
- `all-mpnet-base-v2` (768维) - 更大但更准确

### 3. 缓存优化

- 缓存已处理的文档块
- 避免重复处理相同内容

### 4. 异步处理优化

- 使用消息队列处理文档
- 多个worker并行处理
- 更好的资源隔离

---

## 验证步骤

### 测试文档上传

1. 上传一个中等大小的文档（5-10MB）
2. 监控内存使用情况
3. 检查处理时间
4. 验证文档是否正确处理

### 检查日志

```bash
# 查看内存相关日志
docker logs enterprise-ai-knowledge-base | grep -i "memory\|Memory\|MEMORY"

# 查看处理进度
docker logs enterprise-ai-knowledge-base | grep "Document Processing"
```

---

## 总结

✅ **已完成的优化**:
- 降低批次大小
- 分批保存chunks
- 更频繁的内存清理
- 优化内存监控
- 减小分块大小

✅ **预期效果**:
- 内存占用降低 30-50%
- 处理更稳定
- 支持更大文档

⚠️ **注意事项**:
- 处理速度可能略有下降（但更稳定）
- 需要监控实际效果
- 可根据服务器配置进一步调整

---

**报告生成时间**: 2025-12-09


