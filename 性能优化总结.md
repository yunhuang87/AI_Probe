# 性能优化总结 - 安装sentence-transformers依赖

## 问题描述

知识库服务使用mock embedding模型，导致文档向量化性能较差。

## 解决过程

### 1. 初始状态
- 错误信息：`sentence-transformers not available: cannot import name 'cached_download' from 'huggingface_hub'`
- 使用mock embedding模型
- 性能较差

### 2. 安装依赖
```bash
docker exec enterprise-ai-knowledge-base pip install sentence-transformers torch transformers --no-cache-dir
```

### 3. 版本兼容性问题
- 问题：`sentence-transformers 2.2.2` 与新版 `huggingface_hub` 不兼容
- 错误：`cannot import name 'cached_download' from 'huggingface_hub'`

### 4. 升级版本
```bash
docker exec enterprise-ai-knowledge-base pip install --upgrade sentence-transformers huggingface-hub --no-cache-dir
```
- 升级到：`sentence-transformers 5.1.2`
- 升级到：`huggingface-hub 1.2.1`

### 5. 网络问题
- 模型下载时遇到网络连接问题
- 错误：`Failed to establish a new connection: [Errno 101] Network is unreachable`

### 6. 代码优化
修改 `knowledge-base/src/core/embedding_manager.py`：
- 添加HuggingFace镜像源支持
- 改进错误处理，网络失败时优雅降级到mock模型
- 添加更详细的日志

## 当前状态

### 已完成的优化
1. ✅ 升级sentence-transformers到5.1.2版本
2. ✅ 升级huggingface-hub到1.2.1版本
3. ✅ 改进embedding_manager的错误处理
4. ✅ 添加镜像源支持

### 待解决的问题
1. ⚠️ 网络连接问题：Docker容器可能无法访问huggingface.co
2. ⚠️ 模型下载失败：需要配置镜像源或手动下载模型

## 建议的解决方案

### 方案1：配置HuggingFace镜像源
在docker-compose.yml或环境变量中配置：
```yaml
environment:
  - HF_ENDPOINT=https://hf-mirror.com
```

### 方案2：手动下载模型
在本地下载模型后挂载到容器：
```bash
# 下载模型到本地
python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2')"

# 在docker-compose.yml中挂载
volumes:
  - ~/.cache/huggingface:/root/.cache/huggingface
```

### 方案3：使用本地模型文件
如果已有模型文件，可以直接指定路径：
```python
model = SentenceTransformer('/path/to/local/model')
```

## 性能影响

### Mock模型 vs 真实模型
- **Mock模型**：快速但向量质量差，不适合生产环境
- **真实模型**：速度较慢但向量质量高，适合生产环境

### 优化建议
1. 如果网络可用，优先使用真实模型
2. 配置镜像源以加速下载
3. 考虑使用GPU加速（如果可用）
4. 对于大文档，考虑批量处理优化

## 相关文件

1. `knowledge-base/src/core/embedding_manager.py` - Embedding管理器
2. `docker-compose.yml` - Docker配置
3. `knowledge-base/requirements.txt` - Python依赖

## 下一步

1. 配置HuggingFace镜像源环境变量
2. 验证模型是否可以正常加载
3. 测试文档处理性能提升
4. 监控向量化速度和质量


