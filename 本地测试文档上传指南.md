# 本地测试文档上传指南

## 测试目标
测试知识库服务的文档上传和处理功能，使用 `F5业务蓝图报告_TJJ_SD_V1.1.docx` 文件。

## 前置条件

1. **Docker 和 Docker Compose 已安装并运行**
2. **测试文件存在**：`F5业务蓝图报告_TJJ_SD_V1.1.docx`

## 方法1：使用PowerShell脚本（推荐，Windows）

### 步骤1：启动依赖服务
```powershell
# 启动PostgreSQL和Redis（如果还没启动）
docker-compose up -d postgres redis

# 等待服务就绪（约10秒）
Start-Sleep -Seconds 10
```

### 步骤2：启动知识库服务
```powershell
# 启动知识库服务
docker-compose up -d knowledge-base

# 等待服务启动（约30秒）
Start-Sleep -Seconds 30

# 检查服务是否运行
docker ps | grep knowledge-base
```

### 步骤3：运行测试脚本
```powershell
# 方式1：双击运行
# 双击 "测试文档上传.bat"

# 方式2：PowerShell执行
powershell -ExecutionPolicy Bypass -File "测试文档上传.ps1"
```

脚本会自动：
- 检查Docker和依赖服务
- 启动知识库服务（如果未运行）
- 上传文档
- 监控处理过程（最多10分钟）
- 显示最新日志

## 方法2：使用Python脚本

### 步骤1：安装依赖
```bash
pip install requests
```

### 步骤2：启动服务
```bash
# 启动依赖服务
docker-compose up -d postgres redis

# 启动知识库服务
docker-compose up -d knowledge-base

# 等待服务启动
sleep 30
```

### 步骤3：运行测试
```bash
python test_document_upload.py
```

## 方法3：手动测试

### 1. 启动服务
```bash
docker-compose up -d postgres redis knowledge-base
```

### 2. 等待服务就绪
```bash
# 检查服务状态
docker ps | grep -E "postgres|redis|knowledge-base"

# 检查知识库健康状态
curl http://localhost:8004/api/health
```

### 3. 上传文档
```bash
curl -X POST "http://localhost:8004/api/documents/upload?process_async=true" \
  -F "file=@F5业务蓝图报告_TJJ_SD_V1.1.docx"
```

### 4. 监控处理过程
```bash
# 获取文档ID（从上一步的响应中获取）
DOCUMENT_ID="your-document-id"

# 查询文档状态
curl "http://localhost:8004/api/documents/$DOCUMENT_ID"

# 查看实时日志
docker logs enterprise-ai-knowledge-base -f
```

## 查看日志

### 查看最新日志
```bash
docker logs enterprise-ai-knowledge-base --tail 100
```

### 实时查看日志
```bash
docker logs enterprise-ai-knowledge-base -f
```

### 查看错误日志
```bash
docker logs enterprise-ai-knowledge-base --tail 500 2>&1 | grep -i "error\|exception\|failed\|timeout"
```

## 常见问题排查

### 1. 服务无法启动
```bash
# 检查端口是否被占用
netstat -ano | findstr :8004  # Windows
lsof -i :8004                 # Linux/Mac

# 查看服务日志
docker-compose logs knowledge-base
```

### 2. 上传失败
- 检查文件大小是否超过限制（默认100MB）
- 检查文件格式是否支持（.docx, .pdf, .txt, .md等）
- 查看服务日志了解具体错误

### 3. 处理卡在"parsing"阶段
- 可能是文件太大或格式有问题
- 查看日志中的错误信息
- 检查是否有超时错误

### 4. 处理失败
- 查看日志中的错误堆栈
- 检查数据库连接是否正常
- 检查向量数据库（Chroma）是否正常

## 预期结果

### 成功情况
1. 文档上传成功，返回文档ID
2. 状态从 `processing` 变为 `completed`
3. `total_chunks` > 0（表示文档已成功分块和向量化）
4. 日志中显示处理完成的记录

### 失败情况
1. 状态变为 `failed`
2. 日志中有错误信息
3. `error_message` 字段包含错误详情

## 测试完成后

### 清理测试数据（可选）
```bash
# 查看所有文档
curl "http://localhost:8004/api/documents"

# 删除测试文档（如果需要）
curl -X DELETE "http://localhost:8004/api/documents/{document_id}"
```

### 停止服务（可选）
```bash
docker-compose stop knowledge-base
# 或停止所有服务
docker-compose down
```

## 注意事项

1. **首次启动可能需要较长时间**：需要下载镜像、初始化数据库等
2. **大文件处理时间较长**：根据文件大小，处理可能需要几分钟
3. **资源消耗**：文档处理会消耗CPU和内存，确保有足够资源
4. **日志级别**：如果看不到详细日志，可以设置 `LOG_LEVEL=debug` 环境变量

## 相关文件

- `测试文档上传.ps1` - PowerShell测试脚本
- `测试文档上传.bat` - Windows批处理文件
- `test_document_upload.py` - Python测试脚本
- `F5业务蓝图报告_TJJ_SD_V1.1.docx` - 测试文档

