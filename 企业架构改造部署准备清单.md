# 企业架构改造部署准备清单

**日期**: 2025-12-07  
**状态**: ✅ 测试通过，准备部署

---

## ✅ 本地测试完成情况

### 1. 数据库迁移 ✅
- ✅ 迁移脚本执行成功
- ✅ 5个新表创建成功
- ✅ 5个现有表增强成功

### 2. 模型验证 ✅
- ✅ 所有模型可以正常导入
- ✅ 所有relationship定义正确
- ✅ 模型创建和删除测试通过

### 3. 服务层测试 ✅
- ✅ OrganizationArchitectureService功能正常
- ✅ 所有CRUD操作正常

---

## 📋 部署前检查清单

### 代码文件 ✅
- [x] 数据库迁移脚本 (`026_enhance_enterprise_architecture_models.py`)
- [x] 模型文件更新 (`enterprise_architecture_models.py`)
- [x] 服务文件 (`organization_architecture_service.py`)
- [x] 测试脚本
- [x] 部署脚本 (`deploy_enterprise_architecture.sh`)

### 配置文件 ⚠️
- [ ] 服务器地址配置（需要在部署脚本中设置）
- [ ] SSH密钥文件确认（`remote.ssh` 已找到）
- [ ] 数据库连接信息（服务器上需要配置）

### 服务器环境 ⚠️
- [ ] 应用服务器可访问
- [ ] Neo4j服务器可访问（如果分离部署）
- [ ] 数据库备份已执行
- [ ] 服务停止（部署前）

---

## 🚀 部署步骤

### 步骤1: 配置部署脚本

编辑 `scripts/deploy_enterprise_architecture.sh`，设置以下变量：

```bash
# 应用服务器配置
APP_SERVER_HOST="your-app-server-ip-or-domain"
APP_SERVER_USER="root"  # 或您的SSH用户名
APP_SERVER_KEY="./remote.ssh"  # 密钥文件路径

# Neo4j服务器配置（如果分离部署）
NEO4J_SERVER_HOST="your-neo4j-server-ip-or-domain"
NEO4J_SERVER_USER="root"
NEO4J_SERVER_KEY="./remote.ssh"
```

### 步骤2: 检查密钥文件

```bash
# 检查密钥文件是否存在
ls -la ./remote.ssh

# 设置正确的权限（Linux/Mac）
chmod 600 ./remote.ssh
```

### 步骤3: 测试SSH连接

```bash
# 测试应用服务器连接
ssh -i ./remote.ssh user@your-app-server

# 测试Neo4j服务器连接（如果分离部署）
ssh -i ./remote.ssh user@your-neo4j-server
```

### 步骤4: 运行部署脚本

```bash
# 运行部署脚本
bash scripts/deploy_enterprise_architecture.sh
```

### 步骤5: 验证部署

```bash
# 连接到服务器
ssh -i ./remote.ssh user@your-app-server

# 检查数据库迁移
cd /opt/enterprise-ai-platform/database
python -m alembic current

# 检查新表
psql -U postgres -d enterprise_ai -c "\dt organization_*"
psql -U postgres -d enterprise_ai -c "\dt technology_*"

# 检查服务状态
docker-compose ps
# 或
systemctl status enterprise-ai-platform
```

---

## 📊 验证检查点

### 数据库验证
- [ ] 新表存在（organization_units, technology_instances等）
- [ ] 增强字段存在（organization_id, system_category等）
- [ ] 索引创建成功
- [ ] 外键约束创建成功

### 服务验证
- [ ] 服务可以正常启动
- [ ] 服务日志无错误
- [ ] API端点可访问

### 功能验证
- [ ] 可以创建组织单元
- [ ] 可以查询组织信息
- [ ] 数据库连接正常
- [ ] Neo4j连接正常（如果配置）

---

## ⚠️ 注意事项

### 1. 数据库备份
**重要**: 部署前务必备份数据库！

```bash
# 在服务器上执行
pg_dump -U postgres enterprise_ai > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 2. 服务停止
部署前建议停止相关服务：

```bash
# 停止服务
docker-compose stop metadata-service api-gateway
# 或
systemctl stop enterprise-ai-platform
```

### 3. 回滚计划
如果部署失败，可以回滚：

```bash
# 回滚数据库迁移
cd /opt/enterprise-ai-platform/database
python -m alembic downgrade -1

# 恢复数据库备份
psql -U postgres enterprise_ai < backup_*.sql
```

### 4. 环境变量
确保服务器上配置了正确的环境变量：

```bash
# 数据库连接
DB_HOST=localhost
DB_PORT=5432
DB_USER=ai_user
DB_PASSWORD=ai_password
DB_NAME=ai_platform

# Neo4j连接（如果分离部署）
NEO4J_URI=bolt://your-neo4j-server:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password
```

---

## 📝 部署后验证

### 1. 数据库验证

```sql
-- 检查新表
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN (
    'organization_units',
    'business_roles',
    'technology_types',
    'technology_instances',
    'organization_business_relationships'
);

-- 检查增强字段
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'business_processes' 
AND column_name = 'organization_id';
```

### 2. API验证

```bash
# 测试组织架构API（如果已实现）
curl http://your-server:8000/api/enterprise-architecture/organizations

# 测试健康检查
curl http://your-server:8000/health
```

### 3. 服务日志

```bash
# 查看服务日志
docker-compose logs metadata-service
# 或
journalctl -u enterprise-ai-platform -f
```

---

## ✅ 部署完成标准

- [ ] 数据库迁移成功执行
- [ ] 所有新表创建成功
- [ ] 所有增强字段添加成功
- [ ] 服务正常启动
- [ ] 无错误日志
- [ ] API端点可访问（如果已实现）

---

**准备状态**: ✅ **可以部署**  
**下一步**: 配置服务器信息并运行部署脚本

