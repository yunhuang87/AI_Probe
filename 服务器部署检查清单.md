# 服务器部署检查清单

**日期**: 2025-12-07  
**问题**: 服务器上的菜单配置是旧版本，缺少组织架构等菜单项

---

## 🔍 问题确认

### 从实际页面检查
**URL**: http://43.143.139.197:3000/admin/dashboard

**企业架构菜单显示**:
- ✅ 总览
- ❌ **组织架构** (缺失)
- ✅ 业务架构
- ✅ 应用架构
- ✅ 数据架构
- ✅ 技术架构
- ❌ **技术实例** (缺失)
- ❌ **技术标准化** (缺失)
- ✅ 架构关系图

**结论**: 服务器上的代码是旧版本，只有6个菜单项，缺少3个。

---

## ✅ 本地代码检查

### 菜单配置（本地）
**文件**: `web-ui/src/components/Layout/Sidebar.tsx`

**企业架构菜单配置** (第108-123行):
```typescript
{
  name: '企业架构',
  href: '/enterprise-architecture',
  icon: Building2,
  children: [
    { name: '总览', href: '/enterprise-architecture', icon: LayoutDashboard },
    { name: '组织架构', href: '/enterprise-architecture/organization', icon: Users }, ✅
    { name: '业务架构', href: '/enterprise-architecture/business', icon: Building2 },
    { name: '应用架构', href: '/enterprise-architecture/application', icon: Component },
    { name: '数据架构', href: '/enterprise-architecture/data', icon: Database },
    { name: '技术架构', href: '/enterprise-architecture/technology', icon: Wrench },
    { name: '技术实例', href: '/enterprise-architecture/technology/instances', icon: Server }, ✅
    { name: '技术标准化', href: '/enterprise-architecture/technology/standardization', icon: BarChart3 }, ✅
    { name: '架构关系图', href: '/enterprise-architecture/relationships', icon: Network },
  ],
}
```

**状态**: ✅ 本地代码配置正确，包含所有9个菜单项

---

## 🔧 需要执行的操作

### 1. 重新部署前端代码到服务器

**服务器信息**:
- **应用服务器**: 43.143.139.197:3000
- **需要部署**: `web-ui` 目录下的所有文件

**部署步骤**:
```bash
# 1. 构建前端代码
cd web-ui
npm run build

# 2. 上传到服务器
# 需要将以下文件/目录上传到服务器:
# - web-ui/.next (构建输出)
# - web-ui/src/components/Layout/Sidebar.tsx (菜单配置)
# - web-ui/src/app/enterprise-architecture/** (所有企业架构页面)
```

### 2. 菜单折叠功能修复

**已修复的逻辑**:
- ✅ 默认所有菜单折叠
- ✅ 只有子菜单匹配当前路径时才自动展开
- ✅ 用户可以手动折叠/展开

**需要验证**:
- [ ] 菜单默认折叠
- [ ] 点击可以展开/折叠
- [ ] 折叠状态正确保存

---

## 📋 部署文件清单

### 必须部署的文件
1. **菜单配置**
   - `web-ui/src/components/Layout/Sidebar.tsx`

2. **企业架构页面**
   - `web-ui/src/app/enterprise-architecture/page.tsx`
   - `web-ui/src/app/enterprise-architecture/layout.tsx`
   - `web-ui/src/app/enterprise-architecture/organization/page.tsx` ✅
   - `web-ui/src/app/enterprise-architecture/business/page.tsx`
   - `web-ui/src/app/enterprise-architecture/application/page.tsx`
   - `web-ui/src/app/enterprise-architecture/data/page.tsx`
   - `web-ui/src/app/enterprise-architecture/technology/page.tsx`
   - `web-ui/src/app/enterprise-architecture/technology/instances/page.tsx` ✅
   - `web-ui/src/app/enterprise-architecture/technology/standardization/page.tsx` ✅
   - `web-ui/src/app/enterprise-architecture/relationships/page.tsx`

3. **构建输出**
   - `web-ui/.next/` (需要重新构建)

---

## 🚀 快速部署方案

### 方案1: 完整重新部署
```bash
# 在服务器上
cd /path/to/web-ui
git pull  # 或上传最新代码
npm install
npm run build
# 重启Next.js服务
```

### 方案2: 只更新关键文件
```bash
# 上传以下文件到服务器:
1. web-ui/src/components/Layout/Sidebar.tsx
2. web-ui/src/app/enterprise-architecture/** (所有文件)
3. 重新构建: npm run build
```

---

## ✅ 验证步骤

部署后验证：
1. **菜单项数量**
   - [ ] 企业架构菜单下有9个子菜单项
   - [ ] 包含：总览、组织架构、业务架构、应用架构、数据架构、技术架构、技术实例、技术标准化、架构关系图

2. **菜单折叠**
   - [ ] 所有菜单默认折叠
   - [ ] 点击可以展开/折叠
   - [ ] 折叠状态正确保存

3. **页面访问**
   - [ ] `/enterprise-architecture/organization` 可以正常访问
   - [ ] `/enterprise-architecture/technology/instances` 可以正常访问
   - [ ] `/enterprise-architecture/technology/standardization` 可以正常访问

---

**检查时间**: 2025-12-07  
**状态**: ⚠️ **需要重新部署到服务器**

