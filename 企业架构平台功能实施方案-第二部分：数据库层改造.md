# 企业架构平台功能实施方案

## 第二部分：数据库层改造

**方案日期**: 2025-12-07  
**目标**: 详细说明数据库模型的改造和新增

---

## 📋 一、模型增强

### 1.1 BusinessProcess模型增强

#### 1.1.1 需要添加的字段

```python
# database/src/models/enterprise_architecture_models.py

class BusinessProcess(BaseModel, TimestampMixin):
    """业务流程模型"""
    __tablename__ = "business_processes"
    
    # 现有字段...
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(255), nullable=False, index=True)
    description = Column(Text, nullable=True)
    owner = Column(String(255), nullable=True)
    status = Column(String(50), nullable=True, default="active", index=True)
    classification = Column(String(100), nullable=True)
    level = Column(Integer, nullable=True, default=1)
    parent_id = Column(UUID(as_uuid=True), ForeignKey("business_processes.id", ondelete="SET NULL"), nullable=True, index=True)
    neo4j_node_id = Column(String(255), nullable=True)
    meta_data = Column(JSONB, nullable=True, default=dict)
    
    # ⭐ 新增字段
    organization_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="SET NULL"), nullable=True, index=True, comment="负责组织ID")
    
    # 关系
    parent = relationship("BusinessProcess", remote_side=[id], backref="children")
    organization = relationship("OrganizationUnit", backref="processes")  # ⭐ 新增
```

#### 1.1.2 meta_data字段增强

```python
# 使用示例
process = BusinessProcess(
    name="采购到付款",
    description="从采购申请到付款完成的完整流程",
    owner="张三",
    organization_id=org_unit.id,  # ⭐ 新增
    meta_data={
        "pain_points": ["审批慢", "对账复杂", "供应商管理难"],  # ⭐ 新增
        "criticality": "高",  # ⭐ 新增
        "sap_modules": ["MM", "FI"],  # ⭐ 新增
        "kpi": "采购周期<7天",  # ⭐ 新增
        "industry": "chemical"  # ⭐ 新增
    }
)
```

### 1.2 BusinessCapability模型增强

```python
class BusinessCapability(BaseModel, TimestampMixin):
    """业务能力模型"""
    __tablename__ = "business_capabilities"
    
    # 现有字段...
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(255), nullable=False, index=True)
    description = Column(Text, nullable=True)
    level = Column(Integer, nullable=True, default=1)
    parent_id = Column(UUID(as_uuid=True), ForeignKey("business_capabilities.id", ondelete="SET NULL"), nullable=True, index=True)
    neo4j_node_id = Column(String(255), nullable=True)
    meta_data = Column(JSONB, nullable=True, default=dict)
    
    # ⭐ 新增字段
    owner_organization_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="SET NULL"), nullable=True, index=True, comment="拥有组织ID")
    
    # 关系
    parent = relationship("BusinessCapability", remote_side=[id], backref="children")
    owner_organization = relationship("OrganizationUnit", backref="owned_capabilities")  # ⭐ 新增
```

### 1.3 ApplicationSystem模型增强

```python
class ApplicationSystem(BaseModel, TimestampMixin):
    """应用系统模型"""
    __tablename__ = "application_systems"
    
    # 现有字段...
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(255), nullable=False, index=True)
    description = Column(Text, nullable=True)
    system_type = Column(String(100), nullable=True)
    vendor = Column(String(255), nullable=True)
    version = Column(String(50), nullable=True)
    status = Column(String(50), nullable=True, default="active", index=True)
    owner = Column(String(255), nullable=True)
    neo4j_node_id = Column(String(255), nullable=True)
    meta_data = Column(JSONB, nullable=True, default=dict)
    
    # ⭐ 新增字段
    system_category = Column(String(50), nullable=True, index=True, comment="系统分类: Core, Peripheral, Custom")
    business_owner_org_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="SET NULL"), nullable=True, index=True, comment="业务拥有组织ID")
    
    # 关系
    business_owner_org = relationship("OrganizationUnit", backref="managed_systems")  # ⭐ 新增
```

### 1.4 DataEntity模型增强

```python
class DataEntity(BaseModel, TimestampMixin):
    """数据实体模型"""
    __tablename__ = "data_entities"
    
    # 现有字段...
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(255), nullable=False, index=True)
    description = Column(Text, nullable=True)
    schema = Column(JSONB, nullable=True)
    entity_type = Column(String(100), nullable=True)
    neo4j_node_id = Column(String(255), nullable=True)
    meta_data = Column(JSONB, nullable=True, default=dict)
    
    # ⭐ 新增字段
    code = Column(String(100), nullable=True, unique=True, index=True, comment="实体编码")
    schema_name = Column(String(255), nullable=True, comment="Schema名称")
    table_name = Column(String(255), nullable=True, index=True, comment="表名（如果是数据库表）")
    application_system_id = Column(UUID(as_uuid=True), ForeignKey("application_systems.id", ondelete="SET NULL"), nullable=True, index=True, comment="所属应用系统ID")
    technology_system_id = Column(UUID(as_uuid=True), ForeignKey("technology_instances.id", ondelete="SET NULL"), nullable=True, index=True, comment="存储的技术系统ID（数据库）")
    data_model_id = Column(UUID(as_uuid=True), ForeignKey("data_models.id", ondelete="SET NULL"), nullable=True, index=True, comment="所属数据模型ID")
    
    # 关系
    source_flows = relationship("DataFlow", foreign_keys="DataFlow.source_entity_id", back_populates="source_entity")
    target_flows = relationship("DataFlow", foreign_keys="DataFlow.target_entity_id", back_populates="target_entity")
    application_system = relationship("ApplicationSystem", backref="data_entities")  # ⭐ 新增
    technology_system = relationship("TechnologyInstance", backref="stored_entities")  # ⭐ 新增
    data_model = relationship("DataModel", backref="entities")  # ⭐ 新增
```

### 1.5 APIInterface模型增强

```python
class APIInterface(BaseModel, TimestampMixin):
    """API接口模型"""
    __tablename__ = "api_interfaces"
    
    # 现有字段...
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    name = Column(String(255), nullable=False, index=True)
    path = Column(String(500), nullable=True)
    method = Column(String(50), nullable=True)
    description = Column(Text, nullable=True)
    status = Column(String(50), nullable=True, default="active", index=True)
    neo4j_node_id = Column(String(255), nullable=True)
    meta_data = Column(JSONB, nullable=True, default=dict)
    
    # ⭐ 新增字段
    code = Column(String(100), nullable=True, unique=True, index=True, comment="接口编码")
    interface_type = Column(String(100), nullable=True, index=True, comment="接口类型: REST, SOAP, GraphQL等")
    endpoint = Column(String(500), nullable=True, comment="接口端点（完整URL）")
    application_system_id = Column(UUID(as_uuid=True), ForeignKey("application_systems.id", ondelete="SET NULL"), nullable=True, index=True, comment="所属应用系统ID")
    
    # 关系
    application_system = relationship("ApplicationSystem", backref="api_interfaces")  # ⭐ 新增
```

---

## 🆕 二、新增模型

### 2.1 OrganizationUnit模型 ⭐

```python
class OrganizationUnit(BaseModel, TimestampMixin):
    """组织单元模型"""
    __tablename__ = "organization_units"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="组织ID")
    name = Column(String(255), nullable=False, index=True, comment="组织名称")
    code = Column(String(100), nullable=True, unique=True, index=True, comment="组织编码")
    description = Column(Text, nullable=True, comment="组织描述")
    organization_type = Column(String(100), nullable=True, index=True, comment="组织类型: Department, Team, Division等")
    level = Column(Integer, nullable=True, default=1, comment="组织层级")
    parent_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="SET NULL"), nullable=True, index=True, comment="上级组织ID")
    manager_id = Column(UUID(as_uuid=True), ForeignKey("users.id", ondelete="SET NULL"), nullable=True, index=True, comment="负责人ID")
    location = Column(String(255), nullable=True, comment="地理位置")
    status = Column(String(50), nullable=True, default="active", index=True, comment="状态")
    neo4j_node_id = Column(String(255), nullable=True, comment="Neo4j节点ID")
    meta_data = Column(JSONB, nullable=True, default=dict, comment="扩展元数据")
    
    # 关系
    parent = relationship("OrganizationUnit", remote_side=[id], backref="children")
    manager = relationship("User", foreign_keys=[manager_id], backref="managed_organizations")
    
    __table_args__ = (
        Index('idx_organization_units_name', 'name'),
        Index('idx_organization_units_code', 'code'),
        Index('idx_organization_units_status', 'status'),
        Index('idx_organization_units_type', 'organization_type'),
    )
    
    def __repr__(self):
        return f"<OrganizationUnit(id={self.id}, name={self.name})>"
```

### 2.2 BusinessRole模型 ⭐

```python
class BusinessRole(BaseModel, TimestampMixin):
    """业务角色模型"""
    __tablename__ = "business_roles"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="角色ID")
    name = Column(String(255), nullable=False, index=True, comment="角色名称")
    code = Column(String(100), nullable=True, unique=True, index=True, comment="角色编码")
    description = Column(Text, nullable=True, comment="角色描述")
    role_type = Column(String(100), nullable=True, index=True, comment="角色类型")
    organization_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="SET NULL"), nullable=True, index=True, comment="所属组织ID")
    responsibilities = Column(JSONB, nullable=True, comment="职责列表")
    required_skills = Column(JSONB, nullable=True, comment="所需技能")
    neo4j_node_id = Column(String(255), nullable=True, comment="Neo4j节点ID")
    meta_data = Column(JSONB, nullable=True, default=dict, comment="扩展元数据")
    
    # 关系
    organization = relationship("OrganizationUnit", backref="roles")
    
    __table_args__ = (
        Index('idx_business_roles_name', 'name'),
        Index('idx_business_roles_code', 'code'),
    )
    
    def __repr__(self):
        return f"<BusinessRole(id={self.id}, name={self.name})>"
```

### 2.3 TechnologyInstance模型 ⭐

```python
class TechnologyInstance(BaseModel, TimestampMixin):
    """技术实例模型（按系统分类）"""
    __tablename__ = "technology_instances"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="实例ID")
    name = Column(String(255), nullable=False, index=True, comment="实例名称，如Oracle-SAP-ERP-001")
    instance_id = Column(String(255), nullable=True, unique=True, index=True, comment="实例标识")
    
    # 归属关系
    application_system_id = Column(UUID(as_uuid=True), ForeignKey("application_systems.id", ondelete="SET NULL"), nullable=True, index=True, comment="归属应用系统ID")
    owner_department = Column(String(255), nullable=True, comment="负责部门")
    owner_team = Column(String(255), nullable=True, comment="负责团队")
    
    # 技术属性
    technology_type = Column(String(100), nullable=False, index=True, comment="技术类型: Database, Middleware, Infrastructure, AIPlatform")
    technology_name = Column(String(255), nullable=False, index=True, comment="技术名称: Oracle, Redis, Docker等")
    technology_type_id = Column(UUID(as_uuid=True), ForeignKey("technology_types.id", ondelete="SET NULL"), nullable=True, index=True, comment="关联技术类型ID")
    vendor = Column(String(255), nullable=True, comment="供应商")
    version = Column(String(50), nullable=True, comment="版本")
    
    # 部署属性
    deployment_type = Column(String(50), nullable=True, default="independent", index=True, comment="部署类型: independent(独立), dedicated(专用), shared(共享)")
    host = Column(String(255), nullable=True, comment="主机地址")
    port = Column(Integer, nullable=True, comment="端口")
    location = Column(String(255), nullable=True, comment="部署位置")
    
    # 资源属性
    capacity = Column(String(100), nullable=True, comment="容量，如500GB")
    resource_allocation = Column(String(50), nullable=True, default="dedicated", comment="资源分配: dedicated(专用), shared(共享)")
    
    # 状态
    status = Column(String(50), nullable=True, default="active", index=True, comment="状态: active, inactive, planned, deprecated")
    lifecycle_status = Column(String(50), nullable=True, comment="生命周期状态: strategic, tactical, legacy")
    
    neo4j_node_id = Column(String(255), nullable=True, comment="Neo4j节点ID")
    meta_data = Column(JSONB, nullable=True, default=dict, comment="扩展元数据")
    
    # 关系
    application_system = relationship("ApplicationSystem", backref="technology_instances")
    technology_type = relationship("TechnologyType", backref="instances")
    
    __table_args__ = (
        Index('idx_tech_instances_app', 'application_system_id'),
        Index('idx_tech_instances_tech', 'technology_name', 'technology_type'),
        Index('idx_tech_instances_type', 'technology_type_id'),
        Index('idx_tech_instances_deployment', 'deployment_type'),
    )
    
    def __repr__(self):
        return f"<TechnologyInstance(id={self.id}, name={self.name})>"
```

### 2.4 TechnologyType模型 ⭐

```python
class TechnologyType(BaseModel, TimestampMixin):
    """技术类型模型（按技术分类）"""
    __tablename__ = "technology_types"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="类型ID")
    name = Column(String(255), nullable=False, unique=True, index=True, comment="技术名称，如Oracle")
    category = Column(String(100), nullable=False, index=True, comment="技术类别: Database, Middleware, Infrastructure, AIPlatform")
    description = Column(Text, nullable=True, comment="技术描述")
    
    # 标准化属性
    standard_version = Column(String(50), nullable=True, comment="企业标准版本，如19c")
    recommended_version = Column(String(50), nullable=True, comment="推荐版本")
    lifecycle_status = Column(String(50), nullable=True, default="strategic", index=True, comment="生命周期状态: strategic(战略), tactical(战术), legacy(遗留)")
    
    # 治理属性
    owner_team = Column(String(255), nullable=True, comment="技术负责人团队")
    governance_status = Column(String(50), nullable=True, comment="治理状态: approved(已批准), under_review(审查中), deprecated(已弃用)")
    usage_guidelines = Column(Text, nullable=True, comment="使用指南")
    
    # 统计属性（自动计算）
    instance_count = Column(Integer, nullable=True, default=0, comment="实例数量")
    version_diversity = Column(JSONB, nullable=True, comment="版本多样性统计")
    
    neo4j_node_id = Column(String(255), nullable=True, comment="Neo4j节点ID")
    meta_data = Column(JSONB, nullable=True, default=dict, comment="扩展元数据")
    
    # 关系
    instances = relationship("TechnologyInstance", backref="technology_type_ref")
    
    __table_args__ = (
        Index('idx_tech_types_category', 'category'),
        Index('idx_tech_types_lifecycle', 'lifecycle_status'),
    )
    
    def __repr__(self):
        return f"<TechnologyType(id={self.id}, name={self.name})>"
```

### 2.5 OrganizationBusinessRelationship模型 ⭐

```python
class OrganizationBusinessRelationship(BaseModel, TimestampMixin):
    """组织与业务关系模型"""
    __tablename__ = "organization_business_relationships"
    
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4, comment="关系ID")
    organization_id = Column(UUID(as_uuid=True), ForeignKey("organization_units.id", ondelete="CASCADE"), nullable=False, index=True, comment="组织ID")
    entity_type = Column(String(100), nullable=False, index=True, comment="实体类型: BusinessCapability, BusinessProcess, ApplicationSystem等")
    entity_id = Column(UUID(as_uuid=True), nullable=False, index=True, comment="实体ID")
    relationship_type = Column(String(100), nullable=False, index=True, comment="关系类型: OWNS, EXECUTES, USES, MANAGES等")
    responsibility_level = Column(String(50), nullable=True, comment="责任级别: primary, secondary, supporting")
    neo4j_relationship_id = Column(String(255), nullable=True, comment="Neo4j关系ID")
    meta_data = Column(JSONB, nullable=True, default=dict, comment="扩展元数据")
    
    # 关系
    organization = relationship("OrganizationUnit", backref="business_relationships")
    
    __table_args__ = (
        Index('idx_org_business_rel_org', 'organization_id'),
        Index('idx_org_business_rel_entity', 'entity_type', 'entity_id'),
        Index('idx_org_business_rel_type', 'relationship_type'),
    )
    
    def __repr__(self):
        return f"<OrganizationBusinessRelationship(id={self.id}, org={self.organization_id}, entity={self.entity_type})>"
```

---

## 📝 三、数据库迁移脚本

### 3.1 迁移脚本创建

```python
# database/src/migrations/versions/026_enhance_enterprise_architecture_models.py

"""enhance enterprise architecture models

Revision ID: 026
Revises: 025
Create Date: 2025-12-07

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

# revision identifiers
revision = '026'
down_revision = '025'
branch_labels = None
depends_on = None


def upgrade():
    # ========== 1. 增强BusinessProcess ==========
    op.add_column('business_processes', 
        sa.Column('organization_id', postgresql.UUID(as_uuid=True), nullable=True, comment='负责组织ID'))
    op.create_index('idx_business_processes_org', 'business_processes', ['organization_id'])
    op.create_foreign_key('fk_business_processes_org', 'business_processes', 'organization_units', 
                          ['organization_id'], ['id'], ondelete='SET NULL')
    
    # ========== 2. 增强BusinessCapability ==========
    op.add_column('business_capabilities',
        sa.Column('owner_organization_id', postgresql.UUID(as_uuid=True), nullable=True, comment='拥有组织ID'))
    op.create_index('idx_business_capabilities_org', 'business_capabilities', ['owner_organization_id'])
    op.create_foreign_key('fk_business_capabilities_org', 'business_capabilities', 'organization_units',
                          ['owner_organization_id'], ['id'], ondelete='SET NULL')
    
    # ========== 3. 增强ApplicationSystem ==========
    op.add_column('application_systems',
        sa.Column('system_category', sa.String(50), nullable=True, comment='系统分类: Core, Peripheral, Custom'))
    op.add_column('application_systems',
        sa.Column('business_owner_org_id', postgresql.UUID(as_uuid=True), nullable=True, comment='业务拥有组织ID'))
    op.create_index('idx_application_systems_category', 'application_systems', ['system_category'])
    op.create_index('idx_application_systems_org', 'application_systems', ['business_owner_org_id'])
    op.create_foreign_key('fk_application_systems_org', 'application_systems', 'organization_units',
                          ['business_owner_org_id'], ['id'], ondelete='SET NULL')
    
    # ========== 4. 增强DataEntity ==========
    op.add_column('data_entities',
        sa.Column('code', sa.String(100), nullable=True, comment='实体编码'))
    op.add_column('data_entities',
        sa.Column('schema_name', sa.String(255), nullable=True, comment='Schema名称'))
    op.add_column('data_entities',
        sa.Column('table_name', sa.String(255), nullable=True, comment='表名'))
    op.add_column('data_entities',
        sa.Column('application_system_id', postgresql.UUID(as_uuid=True), nullable=True, comment='所属应用系统ID'))
    op.add_column('data_entities',
        sa.Column('technology_system_id', postgresql.UUID(as_uuid=True), nullable=True, comment='存储的技术系统ID'))
    op.add_column('data_entities',
        sa.Column('data_model_id', postgresql.UUID(as_uuid=True), nullable=True, comment='所属数据模型ID'))
    
    op.create_index('idx_data_entities_code', 'data_entities', ['code'], unique=True)
    op.create_index('idx_data_entities_table', 'data_entities', ['table_name'])
    op.create_index('idx_data_entities_app', 'data_entities', ['application_system_id'])
    op.create_index('idx_data_entities_tech', 'data_entities', ['technology_system_id'])
    
    op.create_foreign_key('fk_data_entities_app', 'data_entities', 'application_systems',
                          ['application_system_id'], ['id'], ondelete='SET NULL')
    op.create_foreign_key('fk_data_entities_tech', 'data_entities', 'technology_instances',
                          ['technology_system_id'], ['id'], ondelete='SET NULL')
    op.create_foreign_key('fk_data_entities_model', 'data_entities', 'data_models',
                          ['data_model_id'], ['id'], ondelete='SET NULL')
    
    # ========== 5. 增强APIInterface ==========
    op.add_column('api_interfaces',
        sa.Column('code', sa.String(100), nullable=True, comment='接口编码'))
    op.add_column('api_interfaces',
        sa.Column('interface_type', sa.String(100), nullable=True, comment='接口类型'))
    op.add_column('api_interfaces',
        sa.Column('endpoint', sa.String(500), nullable=True, comment='接口端点'))
    op.add_column('api_interfaces',
        sa.Column('application_system_id', postgresql.UUID(as_uuid=True), nullable=True, comment='所属应用系统ID'))
    
    op.create_index('idx_api_interfaces_code', 'api_interfaces', ['code'], unique=True)
    op.create_index('idx_api_interfaces_type', 'api_interfaces', ['interface_type'])
    op.create_index('idx_api_interfaces_app', 'api_interfaces', ['application_system_id'])
    
    op.create_foreign_key('fk_api_interfaces_app', 'api_interfaces', 'application_systems',
                          ['application_system_id'], ['id'], ondelete='SET NULL')
    
    # ========== 6. 创建OrganizationUnit表 ==========
    op.create_table(
        'organization_units',
        sa.Column('id', postgresql.UUID(as_uuid=True), primary_key=True, default=sa.text('gen_random_uuid()')),
        sa.Column('name', sa.String(255), nullable=False),
        sa.Column('code', sa.String(100), nullable=True),
        sa.Column('description', sa.Text(), nullable=True),
        sa.Column('organization_type', sa.String(100), nullable=True),
        sa.Column('level', sa.Integer(), nullable=True, default=1),
        sa.Column('parent_id', postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column('manager_id', postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column('location', sa.String(255), nullable=True),
        sa.Column('status', sa.String(50), nullable=True, default='active'),
        sa.Column('neo4j_node_id', sa.String(255), nullable=True),
        sa.Column('meta_data', postgresql.JSONB(), nullable=True, default=dict),
        sa.Column('created_at', sa.DateTime(), nullable=True),
        sa.Column('updated_at', sa.DateTime(), nullable=True),
        sa.ForeignKeyConstraint(['parent_id'], ['organization_units.id'], ondelete='SET NULL'),
        sa.ForeignKeyConstraint(['manager_id'], ['users.id'], ondelete='SET NULL'),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('idx_organization_units_name', 'organization_units', ['name'])
    op.create_index('idx_organization_units_code', 'organization_units', ['code'], unique=True)
    op.create_index('idx_organization_units_status', 'organization_units', ['status'])
    op.create_index('idx_organization_units_type', 'organization_units', ['organization_type'])
    
    # ========== 7. 创建BusinessRole表 ==========
    op.create_table(
        'business_roles',
        sa.Column('id', postgresql.UUID(as_uuid=True), primary_key=True, default=sa.text('gen_random_uuid()')),
        sa.Column('name', sa.String(255), nullable=False),
        sa.Column('code', sa.String(100), nullable=True),
        sa.Column('description', sa.Text(), nullable=True),
        sa.Column('role_type', sa.String(100), nullable=True),
        sa.Column('organization_id', postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column('responsibilities', postgresql.JSONB(), nullable=True),
        sa.Column('required_skills', postgresql.JSONB(), nullable=True),
        sa.Column('neo4j_node_id', sa.String(255), nullable=True),
        sa.Column('meta_data', postgresql.JSONB(), nullable=True, default=dict),
        sa.Column('created_at', sa.DateTime(), nullable=True),
        sa.Column('updated_at', sa.DateTime(), nullable=True),
        sa.ForeignKeyConstraint(['organization_id'], ['organization_units.id'], ondelete='SET NULL'),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('idx_business_roles_name', 'business_roles', ['name'])
    op.create_index('idx_business_roles_code', 'business_roles', ['code'], unique=True)
    
    # ========== 8. 创建TechnologyType表 ==========
    op.create_table(
        'technology_types',
        sa.Column('id', postgresql.UUID(as_uuid=True), primary_key=True, default=sa.text('gen_random_uuid()')),
        sa.Column('name', sa.String(255), nullable=False, unique=True),
        sa.Column('category', sa.String(100), nullable=False),
        sa.Column('description', sa.Text(), nullable=True),
        sa.Column('standard_version', sa.String(50), nullable=True),
        sa.Column('recommended_version', sa.String(50), nullable=True),
        sa.Column('lifecycle_status', sa.String(50), nullable=True, default='strategic'),
        sa.Column('owner_team', sa.String(255), nullable=True),
        sa.Column('governance_status', sa.String(50), nullable=True),
        sa.Column('usage_guidelines', sa.Text(), nullable=True),
        sa.Column('instance_count', sa.Integer(), nullable=True, default=0),
        sa.Column('version_diversity', postgresql.JSONB(), nullable=True),
        sa.Column('neo4j_node_id', sa.String(255), nullable=True),
        sa.Column('meta_data', postgresql.JSONB(), nullable=True, default=dict),
        sa.Column('created_at', sa.DateTime(), nullable=True),
        sa.Column('updated_at', sa.DateTime(), nullable=True),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('idx_tech_types_category', 'technology_types', ['category'])
    op.create_index('idx_tech_types_lifecycle', 'technology_types', ['lifecycle_status'])
    
    # ========== 9. 创建TechnologyInstance表 ==========
    op.create_table(
        'technology_instances',
        sa.Column('id', postgresql.UUID(as_uuid=True), primary_key=True, default=sa.text('gen_random_uuid()')),
        sa.Column('name', sa.String(255), nullable=False),
        sa.Column('instance_id', sa.String(255), nullable=True, unique=True),
        sa.Column('application_system_id', postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column('owner_department', sa.String(255), nullable=True),
        sa.Column('owner_team', sa.String(255), nullable=True),
        sa.Column('technology_type', sa.String(100), nullable=False),
        sa.Column('technology_name', sa.String(255), nullable=False),
        sa.Column('technology_type_id', postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column('vendor', sa.String(255), nullable=True),
        sa.Column('version', sa.String(50), nullable=True),
        sa.Column('deployment_type', sa.String(50), nullable=True, default='independent'),
        sa.Column('host', sa.String(255), nullable=True),
        sa.Column('port', sa.Integer(), nullable=True),
        sa.Column('location', sa.String(255), nullable=True),
        sa.Column('capacity', sa.String(100), nullable=True),
        sa.Column('resource_allocation', sa.String(50), nullable=True, default='dedicated'),
        sa.Column('status', sa.String(50), nullable=True, default='active'),
        sa.Column('lifecycle_status', sa.String(50), nullable=True),
        sa.Column('neo4j_node_id', sa.String(255), nullable=True),
        sa.Column('meta_data', postgresql.JSONB(), nullable=True, default=dict),
        sa.Column('created_at', sa.DateTime(), nullable=True),
        sa.Column('updated_at', sa.DateTime(), nullable=True),
        sa.ForeignKeyConstraint(['application_system_id'], ['application_systems.id'], ondelete='SET NULL'),
        sa.ForeignKeyConstraint(['technology_type_id'], ['technology_types.id'], ondelete='SET NULL'),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('idx_tech_instances_app', 'technology_instances', ['application_system_id'])
    op.create_index('idx_tech_instances_tech', 'technology_instances', ['technology_name', 'technology_type'])
    op.create_index('idx_tech_instances_type', 'technology_instances', ['technology_type_id'])
    op.create_index('idx_tech_instances_deployment', 'technology_instances', ['deployment_type'])
    
    # ========== 10. 创建OrganizationBusinessRelationship表 ==========
    op.create_table(
        'organization_business_relationships',
        sa.Column('id', postgresql.UUID(as_uuid=True), primary_key=True, default=sa.text('gen_random_uuid()')),
        sa.Column('organization_id', postgresql.UUID(as_uuid=True), nullable=False),
        sa.Column('entity_type', sa.String(100), nullable=False),
        sa.Column('entity_id', postgresql.UUID(as_uuid=True), nullable=False),
        sa.Column('relationship_type', sa.String(100), nullable=False),
        sa.Column('responsibility_level', sa.String(50), nullable=True),
        sa.Column('neo4j_relationship_id', sa.String(255), nullable=True),
        sa.Column('meta_data', postgresql.JSONB(), nullable=True, default=dict),
        sa.Column('created_at', sa.DateTime(), nullable=True),
        sa.Column('updated_at', sa.DateTime(), nullable=True),
        sa.ForeignKeyConstraint(['organization_id'], ['organization_units.id'], ondelete='CASCADE'),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('idx_org_business_rel_org', 'organization_business_relationships', ['organization_id'])
    op.create_index('idx_org_business_rel_entity', 'organization_business_relationships', ['entity_type', 'entity_id'])
    op.create_index('idx_org_business_rel_type', 'organization_business_relationships', ['relationship_type'])


def downgrade():
    # 删除新增的表
    op.drop_table('organization_business_relationships')
    op.drop_table('technology_instances')
    op.drop_table('technology_types')
    op.drop_table('business_roles')
    op.drop_table('organization_units')
    
    # 删除增强的字段
    op.drop_constraint('fk_api_interfaces_app', 'api_interfaces', type_='foreignkey')
    op.drop_index('idx_api_interfaces_app', 'api_interfaces')
    op.drop_index('idx_api_interfaces_type', 'api_interfaces')
    op.drop_index('idx_api_interfaces_code', 'api_interfaces')
    op.drop_column('api_interfaces', 'application_system_id')
    op.drop_column('api_interfaces', 'endpoint')
    op.drop_column('api_interfaces', 'interface_type')
    op.drop_column('api_interfaces', 'code')
    
    op.drop_constraint('fk_data_entities_model', 'data_entities', type_='foreignkey')
    op.drop_constraint('fk_data_entities_tech', 'data_entities', type_='foreignkey')
    op.drop_constraint('fk_data_entities_app', 'data_entities', type_='foreignkey')
    op.drop_index('idx_data_entities_tech', 'data_entities')
    op.drop_index('idx_data_entities_app', 'data_entities')
    op.drop_index('idx_data_entities_table', 'data_entities')
    op.drop_index('idx_data_entities_code', 'data_entities')
    op.drop_column('data_entities', 'data_model_id')
    op.drop_column('data_entities', 'technology_system_id')
    op.drop_column('data_entities', 'application_system_id')
    op.drop_column('data_entities', 'table_name')
    op.drop_column('data_entities', 'schema_name')
    op.drop_column('data_entities', 'code')
    
    op.drop_constraint('fk_application_systems_org', 'application_systems', type_='foreignkey')
    op.drop_index('idx_application_systems_org', 'application_systems')
    op.drop_index('idx_application_systems_category', 'application_systems')
    op.drop_column('application_systems', 'business_owner_org_id')
    op.drop_column('application_systems', 'system_category')
    
    op.drop_constraint('fk_business_capabilities_org', 'business_capabilities', type_='foreignkey')
    op.drop_index('idx_business_capabilities_org', 'business_capabilities')
    op.drop_column('business_capabilities', 'owner_organization_id')
    
    op.drop_constraint('fk_business_processes_org', 'business_processes', type_='foreignkey')
    op.drop_index('idx_business_processes_org', 'business_processes')
    op.drop_column('business_processes', 'organization_id')
```

---

## ✅ 四、模型更新检查清单

### 4.1 模型增强检查

```yaml
- [ ] BusinessProcess添加organization_id字段
- [ ] BusinessCapability添加owner_organization_id字段
- [ ] ApplicationSystem添加system_category和business_owner_org_id字段
- [ ] DataEntity添加code, schema_name, table_name, application_system_id, technology_system_id字段
- [ ] APIInterface添加code, interface_type, endpoint, application_system_id字段
```

### 4.2 新增模型检查

```yaml
- [ ] OrganizationUnit模型创建
- [ ] BusinessRole模型创建
- [ ] TechnologyInstance模型创建
- [ ] TechnologyType模型创建
- [ ] OrganizationBusinessRelationship模型创建
```

### 4.3 迁移脚本检查

```yaml
- [ ] 迁移脚本创建完成
- [ ] 迁移脚本测试通过
- [ ] 回滚脚本测试通过
- [ ] 索引创建完成
- [ ] 外键约束创建完成
```

---

**第二部分完成**  
**下一部分**: 服务层实现

