version: '3.8'
services:
  workflow-engine:
    image: enterprise-ai-platform-workflow-engine:latest
    # 注释掉原有的build部分
    # build:
    #   context: .
    #   dockerfile: workflow-engine/Dockerfile.dev
    #   args:
    #     - PYTHON_VERSION=${PYTHON_VERSION:-3.11}
