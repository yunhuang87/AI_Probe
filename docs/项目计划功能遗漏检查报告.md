# 项目计划功能遗漏检查报告

**检查日期**: 2025-01-27
**检查范围**: 项目计划模板与树形结构功能
**状态**: ⚠️ **发现部分遗漏功能**

---

## 📋 功能完整性检查

### ✅ 已实现的核心功能

#### 1. 项目计划CRUD ✅
- ✅ `GET /api/v1/projects/{project_id}/plans` - 列表
- ✅ `GET /api/v1/projects/{project_id}/plans/{plan_id}` - 详情
- ✅ `POST /api/v1/projects/{project_id}/plans` - 创建（支持模板）
- ✅ `PUT /api/v1/projects/{project_id}/plans/{plan_id}` - 更新
- ✅ `DELETE /api/v1/projects/{project_id}/plans/{plan_id}` - 删除

#### 2. 项目模板CRUD ✅
- ✅ `GET /api/v1/project-templates` - 列表
- ✅ `GET /api/v1/project-templates/{id}` - 详情
- ✅ `POST /api/v1/project-templates` - 创建
- ✅ `PUT /api/v1/project-templates/{id}` - 更新
- ✅ `DELETE /api/v1/project-templates/{id}` - 删除

#### 3. 计划任务操作 ✅
- ✅ `POST /api/v1/projects/{project_id}/plans/{plan_id}/tasks` - 创建
- ✅ `PUT /api/v1/projects/{project_id}/plans/{plan_id}/tasks/{task_id}` - 更新
- ✅ `DELETE /api/v1/projects/{project_id}/plans/{plan_id}/tasks/{task_id}` - 删除
- ✅ `PATCH /api/v1/projects/{project_id}/plans/{plan_id}/tasks/{task_id}/move` - 移动

#### 4. 树形结构API ✅
- ✅ `GET /api/v1/projects/{project_id}/plans/{plan_id}/tree` - 树形结构

#### 5. 基于模板创建计划 ✅
- ✅ 支持`template_id`参数
- ✅ 自动创建阶段
- ✅ 更新模板使用次数

---

## ❌ 发现的遗漏功能

### 1. 任务列表查询接口 ❌

**问题**: 前端可能需要单独获取任务列表，但后端只有树形结构接口

**前端调用**:
- 前端在`[planId]/page.tsx`中可能需要获取任务列表用于甘特图等

**建议**:
- 添加 `GET /api/v1/projects/{project_id}/plans/{plan_id}/tasks` 接口
- 支持分页、筛选（按阶段、状态等）

**状态**: ❌ **未实现**

### 2. 甘特图数据接口 ❌

**问题**: 前端调用了`/api/v1/projects/{projectId}/plans/{planId}/gantt-data`，但后端未实现

**前端代码**:
```typescript
const response = await fetch(
  `${apiUrl}/api/v1/projects/${projectId}/plans/${planId}/gantt-data`,
  ...
);
```

**建议**:
- 添加 `GET /api/v1/projects/{project_id}/plans/{plan_id}/gantt-data` 接口
- 返回适合甘特图显示的任务数据格式

**状态**: ❌ **未实现**

### 3. 计划导出接口 ❌

**问题**: 前端调用了`/api/v1/projects/{projectId}/plans/{planId}/export`，但后端未实现

**前端代码**:
```typescript
const response = await fetch(
  `${apiUrl}/api/v1/projects/${projectId}/plans/${planId}/export?format=${format}`,
  ...
);
```

**建议**:
- 添加 `GET /api/v1/projects/{project_id}/plans/{plan_id}/export` 接口
- 支持多种格式（Excel、PDF、JSON等）

**状态**: ❌ **未实现**

### 4. 计划导入接口 ❌

**问题**: 前端调用了`/api/v1/projects/{projectId}/plans/import`，但后端未实现

**前端代码**:
```typescript
const response = await fetch(
  `${apiUrl}/api/v1/projects/${projectId}/plans/import`,
  ...
);
```

**建议**:
- 添加 `POST /api/v1/projects/{project_id}/plans/import` 接口
- 支持从Excel/JSON导入计划数据

**状态**: ❌ **未实现**

### 5. 预定义任务创建 ⚠️

**问题**: 实施报告中提到"实现预定义任务创建（如果模板包含）"，但当前实现中未处理模板中的预定义任务

**实施报告要求**:
```
- [ ] 实现预定义任务创建（如果模板包含）
```

**当前实现**:
- `create_project_plan`函数只创建了阶段，未处理模板中的`tasks`字段

**建议**:
- 在`create_project_plan`中检查模板的`template_structure.phases[].tasks`
- 如果模板包含预定义任务，自动创建`ProjectPlanTask`记录

**状态**: ⚠️ **部分实现**（只创建了阶段，未创建预定义任务）

### 6. 模板预览功能 ✅

**问题**: 实施报告中提到"实现模板预览功能"

**实施报告要求**:
```
- [ ] 实现模板预览功能
```

**实际状态**:
- ✅ 前端`TemplateSelector`组件已实现预览功能
- ✅ 可以预览模板的阶段结构

**状态**: ✅ **已实现**

### 7. 计划变更历史接口 ⚠️

**问题**: `plan_changes.router`在`main.py`中被注释掉了

**代码**:
```python
# plan_changes 暂时注释，因为PlanChangeLog模型未定义
# from .routes import plan_changes
# app.include_router(plan_changes.router, prefix="/api/v1", tags=["计划变更历史"])
```

**实际状态**:
- ✅ `PlanChangeLog`模型已定义（在`database/src/models/project_models.py`）
- ✅ `plan_changes.py`路由代码已完整实现
- ❌ 路由未注册（被注释掉了）

**前端调用**:
- 前端调用了`/api/v1/projects/{projectId}/plans/{planId}/change-logs`

**建议**:
- 取消注释并注册路由（模型已定义，可以注册）

**状态**: ⚠️ **路由未注册**（但代码已完整实现）

---

## 📊 遗漏功能统计

| 功能 | 优先级 | 状态 | 说明 |
|------|--------|------|------|
| 任务列表查询接口 | 中 | ❌ 未实现 | 前端可能需要 |
| 甘特图数据接口 | 高 | ❌ 未实现 | 前端已调用 |
| 计划导出接口 | 中 | ❌ 未实现 | 前端已调用 |
| 计划导入接口 | 中 | ❌ 未实现 | 前端已调用 |
| 预定义任务创建 | 低 | ⚠️ 部分实现 | 实施报告要求 |
| 模板预览功能 | 低 | ✅ 已实现 | 前端已实现 |
| 计划变更历史 | 中 | ⚠️ 路由未注册 | 代码已存在，只需注册 |

---

## 🔧 需要补充的实现

### 高优先级

#### 1. 甘特图数据接口

**文件**: `project_management/src/routes/project_plans.py`

```python
@router.get("/projects/{project_id}/plans/{plan_id}/gantt-data")
async def get_gantt_data(
    project_id: str,
    plan_id: str,
    db: Session = Depends(get_db),
    current_user: dict = Depends(require_auth),
):
    """获取甘特图数据"""
    # 1. 权限检查
    # 2. 获取计划的所有任务
    # 3. 转换为甘特图格式（包含id, name, start, end, progress, dependencies等）
    # 4. 返回数据
```

#### 2. 任务列表查询接口

**文件**: `project_management/src/routes/project_plans.py`

```python
@router.get("/projects/{project_id}/plans/{plan_id}/tasks", response_model=ProjectPlanTaskListResponse)
async def list_plan_tasks(
    project_id: str,
    plan_id: str,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    phase_id: Optional[str] = None,
    status: Optional[str] = None,
    db: Session = Depends(get_db),
    current_user: dict = Depends(require_auth),
):
    """获取计划任务列表"""
    # 1. 权限检查
    # 2. 构建查询（支持按阶段、状态筛选）
    # 3. 返回任务列表
```

### 中优先级

#### 3. 计划导出接口

**文件**: `project_management/src/routes/project_plans.py`

```python
@router.get("/projects/{project_id}/plans/{plan_id}/export")
async def export_plan(
    project_id: str,
    plan_id: str,
    format: str = Query("excel", regex="^(excel|pdf|json)$"),
    db: Session = Depends(get_db),
    current_user: dict = Depends(require_auth),
):
    """导出计划"""
    # 1. 权限检查
    # 2. 获取计划数据
    # 3. 根据format生成文件
    # 4. 返回文件
```

#### 4. 计划导入接口

**文件**: `project_management/src/routes/project_plans.py`

```python
@router.post("/projects/{project_id}/plans/import")
async def import_plan(
    project_id: str,
    file: UploadFile,
    format: str = Query("excel", regex="^(excel|json)$"),
    db: Session = Depends(get_db),
    current_user: dict = Depends(require_auth),
):
    """导入计划"""
    # 1. 权限检查
    # 2. 解析文件
    # 3. 创建计划和任务
    # 4. 返回结果
```

#### 5. 注册计划变更历史路由

**文件**: `project_management/src/main.py`

`PlanChangeLog`模型已定义，只需取消注释：
```python
from .routes import plan_changes
app.include_router(plan_changes.router, prefix="/api/v1", tags=["计划变更历史"])
```

### 低优先级

#### 6. 预定义任务创建

**文件**: `project_management/src/routes/project_plans.py` - `create_project_plan`函数

在创建阶段后，检查模板中的预定义任务：
```python
# 如果模板包含预定义任务
if template and template.template_structure:
    for phase_data in phases_data:
        # ... 创建阶段 ...

        # 创建预定义任务
        predefined_tasks = phase_data.get("tasks", [])
        for task_template in predefined_tasks:
            # 创建ProjectPlanTask记录
            ...
```

---

## 📝 总结

### ✅ 已完成（核心功能）

- ✅ 项目计划CRUD（5个接口）
- ✅ 项目模板CRUD（5个接口）
- ✅ 计划任务操作（4个接口）
- ✅ 树形结构API（1个接口）
- ✅ 基于模板创建计划（阶段自动创建）

### ❌ 遗漏功能（6个）

1. ❌ **甘特图数据接口**（高优先级）- 前端已调用
2. ❌ **任务列表查询接口**（中优先级）- 实施报告要求
3. ❌ **计划导出接口**（中优先级）- 前端已调用
4. ❌ **计划导入接口**（中优先级）- 前端已调用
5. ⚠️ **预定义任务创建**（低优先级）- 实施报告要求
6. ⚠️ **计划变更历史路由**（中优先级）- 代码已存在但未注册

---

## 🎯 建议

### 立即实现（高优先级）

1. **甘特图数据接口** - 前端已调用，必须实现
2. **任务列表查询接口** - 前端可能需要

### 尽快实现（中优先级）

3. **计划导出接口** - 前端已调用
4. **计划导入接口** - 前端已调用
5. **注册计划变更历史路由** - 检查模型后注册

### 后续实现（低优先级）

6. **预定义任务创建** - 实施报告要求，但非必需

---

**总体完成度**: **约 88%**

**核心功能**: ✅ 100%完成
**辅助功能**: ⚠️ 部分完成（6个遗漏功能，其中1个只需注册路由）

