# OpenCode 环境变量 - 复制为 .env 后按需修改
# 必填：工作目录（与 19 服务同机时填项目根目录，供 /workspace 挂载）
OPENCODE_WORKSPACE=/opt/enterprise-ai-platform

# 必填（与 19 服务同机时）：主 compose 创建的网络名通常带前缀，先执行 docker network ls 查看
# 例如: enterprise-ai-platform_enterprise-ai-network 或 intl-tst-mcp_enterprise-ai-network
EXTERNAL_NETWORK_NAME=enterprise-ai-platform_enterprise-ai-network

# 可选
OPENCODE_PORT=4096
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=12345678

# 模型列表加载（内网/离线时必配，否则无法加载模型）
# 容器默认从 https://models.dev 拉取模型列表；内网无法访问时会导致模型列表为空。
# 做法：在有外网机器下载 https://models.dev/api.json 另存为 models.json，拷贝到服务器后挂载进容器并设置本变量。
# 示例：挂载 ./models.json 到容器 /opt/opencode-models.json，则填 OPENCODE_MODELS_PATH=/opt/opencode-models.json
# OPENCODE_MODELS_PATH=/opt/opencode-models.json
# 内网代理地址（若公司有 models.dev 镜像可填）
# OPENCODE_MODELS_URL=https://models.dev
# 设为 1 时禁止从网络拉取模型列表（仅用 OPENCODE_MODELS_PATH 或镜像内缓存）
# OPENCODE_DISABLE_MODELS_FETCH=0

# 中化集团模型服务（OpenCode 中可选 sinochem/Qwen3-32B-128K），Bearer Token
# SINOCHEM_API_KEY=84b9bbc0-6eca-4650-9be8-f0affa9135d3
