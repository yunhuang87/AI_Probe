# 企业架构数据分离实施方案

## 📋 执行摘要

**目标**: 将企业架构数据按照数据特性分离存储到不同的数据库，实现最优的数据存储和查询性能。

**原则**: 
- **PostgreSQL**: 存储实体元数据（结构化、需要事务支持）
- **Neo4j**: 存储关系数据（图结构、需要图遍历）
- **Qdrant**: 存储向量数据（语义搜索、相似度查询）

---

## 1. 数据分类与存储策略

### 1.1 数据分类矩阵

| 数据类型 | 存储位置 | 原因 | 查询特点 |
|---------|---------|------|---------|
| **实体元数据** | PostgreSQL | 结构化数据，需要ACID事务 | SQL查询、聚合统计 |
| **关系数据** | Neo4j | 图结构，需要图遍历 | Cypher查询、路径发现 |
| **向量数据** | Qdrant | 高维向量，需要相似度搜索 | 向量相似度查询 |

### 1.2 企业架构数据分类

#### 1.2.1 PostgreSQL 存储（实体元数据）

**组织架构**:
- `organization_units` - 组织单元元数据
- `business_roles` - 业务角色元数据

**业务架构**:
- `business_processes` - 业务流程元数据
- `business_capabilities` - 业务能力元数据
- `business_services` - 业务服务元数据

**应用架构**:
- `application_systems` - 应用系统元数据
- `application_services` - 应用服务元数据
- `api_interfaces` - API接口元数据

**数据架构**:
- `data_entities` - 数据实体元数据
- `data_models` - 数据模型元数据
- `data_flows` - 数据流元数据

**技术架构**:
- `technology_types` - 技术类型元数据
- `technology_instances` - 技术实例元数据
- `technology_components` - 技术组件元数据
- `technology_stacks` - 技术栈元数据
- `infrastructure_components` - 基础设施组件元数据

**关联字段**:
- 每个表都有 `neo4j_node_id` 字段，用于关联Neo4j中的节点

#### 1.2.2 Neo4j 存储（关系数据）

**节点类型**:
```cypher
// 组织架构节点
(:OrganizationUnit {uuid, name, code, type, level})
(:BusinessRole {uuid, name, description})

// 业务架构节点
(:BusinessProcess {uuid, name, description, owner})
(:BusinessCapability {uuid, name, level})
(:BusinessService {uuid, name, endpoint})

// 应用架构节点
(:ApplicationSystem {uuid, name, type, status, category})
(:ApplicationService {uuid, name, protocol})
(:APIInterface {uuid, path, method})

// 数据架构节点
(:DataEntity {uuid, name, schema, code})
(:DataModel {uuid, name, version})
(:DataFlow {uuid, source, target})

// 技术架构节点
(:TechnologyType {uuid, name, category})
(:TechnologyInstance {uuid, name, instance_id, version})
(:TechnologyComponent {uuid, name, version})
```

**关系类型**:
```cypher
// 组织关系
(:OrganizationUnit)-[:PARENT_OF]->(:OrganizationUnit)
(:OrganizationUnit)-[:HAS_ROLE]->(:BusinessRole)
(:OrganizationUnit)-[:OWNS]->(:BusinessProcess)
(:OrganizationUnit)-[:OWNS]->(:BusinessCapability)
(:OrganizationUnit)-[:OWNS]->(:ApplicationSystem)

// 业务关系
(:BusinessProcess)-[:REALIZES]->(:BusinessCapability)
(:BusinessProcess)-[:USES]->(:BusinessService)
(:BusinessProcess)-[:SUPPORTED_BY]->(:ApplicationSystem)

// 应用关系
(:ApplicationSystem)-[:PROVIDES]->(:ApplicationService)
(:ApplicationSystem)-[:EXPOSES]->(:APIInterface)
(:ApplicationSystem)-[:USES]->(:DataEntity)
(:ApplicationSystem)-[:DEPENDS_ON]->(:ApplicationSystem)
(:ApplicationSystem)-[:USES_TECHNOLOGY]->(:TechnologyInstance)

// 数据关系
(:DataEntity)-[:PART_OF]->(:DataModel)
(:DataEntity)-[:FLOWS_TO]->(:DataEntity)
(:DataFlow)-[:CONNECTS]->(:DataEntity)

// 技术关系
(:TechnologyInstance)-[:INSTANCE_OF]->(:TechnologyType)
(:TechnologyInstance)-[:USED_BY]->(:ApplicationSystem)
(:TechnologyComponent)-[:PART_OF]->(:TechnologyStack)

// 跨层关系
(:BusinessProcess)-[:IMPLEMENTED_BY]->(:ApplicationSystem)
(:ApplicationSystem)-[:STORES]->(:DataEntity)
(:DataEntity)-[:STORED_IN]->(:TechnologyInstance)
```

#### 1.2.3 Qdrant 存储（向量数据，可选）

**适用场景**:
- 企业架构文档的语义搜索
- 相似业务流程推荐
- 技术栈相似度分析

**集合设计**:
- `enterprise_architecture_documents` - 企业架构文档向量
- `business_processes_vectors` - 业务流程描述向量
- `application_systems_vectors` - 应用系统描述向量

---

## 2. 数据同步策略

### 2.1 双写策略

**写入流程**:
```
用户创建/更新实体
  ↓
1. 写入PostgreSQL（实体元数据）
  ↓
2. 写入Neo4j（节点和关系）
  ↓
3. 更新PostgreSQL的neo4j_node_id字段
```

### 2.2 数据一致性保证

**策略**:
1. **事务保证**: PostgreSQL写入成功后，再写入Neo4j
2. **失败回滚**: Neo4j写入失败时，回滚PostgreSQL事务
3. **异步补偿**: 定期检查并修复不一致数据

### 2.3 查询策略

**实体查询**:
- 从PostgreSQL查询实体元数据
- 使用`neo4j_node_id`关联Neo4j节点

**关系查询**:
- 从Neo4j查询关系数据
- 使用`uuid`关联PostgreSQL实体

**混合查询**:
- 先查询Neo4j获取关系路径
- 再查询PostgreSQL获取实体详情

---

## 3. 实施步骤

### 3.1 第一阶段：数据模型设计

**任务**:
1. ✅ 完善PostgreSQL实体模型（已完成）
2. ✅ 设计Neo4j节点和关系模型（已完成）
3. 创建数据同步服务

### 3.2 第二阶段：数据同步服务实现

**任务**:
1. 创建`EnterpriseArchitectureSyncService`
2. 实现PostgreSQL到Neo4j的同步
3. 实现Neo4j到PostgreSQL的关联更新

### 3.3 第三阶段：数据迁移

**任务**:
1. 创建数据迁移脚本
2. 迁移现有数据到Neo4j
3. 验证数据一致性

### 3.4 第四阶段：查询优化

**任务**:
1. 优化PostgreSQL查询（实体元数据）
2. 优化Neo4j查询（关系数据）
3. 实现混合查询接口

---

## 4. 技术实现

### 4.1 数据同步服务接口

```python
class EnterpriseArchitectureSyncService:
    """企业架构数据同步服务"""
    
    async def sync_entity_to_neo4j(
        self, 
        entity_type: str, 
        entity_id: UUID,
        entity_data: Dict
    ) -> str:
        """同步实体到Neo4j，返回neo4j_node_id"""
        pass
    
    async def sync_relationship_to_neo4j(
        self,
        source_id: UUID,
        target_id: UUID,
        relationship_type: str,
        properties: Dict = None
    ):
        """同步关系到Neo4j"""
        pass
    
    async def update_postgres_neo4j_id(
        self,
        entity_type: str,
        entity_id: UUID,
        neo4j_node_id: str
    ):
        """更新PostgreSQL中的neo4j_node_id"""
        pass
```

### 4.2 查询服务接口

```python
class EnterpriseArchitectureQueryService:
    """企业架构查询服务"""
    
    async def get_entity_with_relationships(
        self,
        entity_type: str,
        entity_id: UUID,
        depth: int = 2
    ) -> Dict:
        """获取实体及其关系"""
        # 1. 从PostgreSQL获取实体元数据
        # 2. 从Neo4j获取关系数据
        # 3. 合并返回
        pass
    
    async def find_impact_scope(
        self,
        entity_id: UUID,
        entity_type: str
    ) -> Dict:
        """影响范围分析（使用Neo4j图遍历）"""
        pass
```

---

## 5. 数据迁移计划

### 5.1 迁移脚本设计

**脚本功能**:
1. 读取PostgreSQL中的企业架构数据
2. 创建Neo4j节点和关系
3. 更新PostgreSQL的`neo4j_node_id`字段
4. 验证数据一致性

### 5.2 迁移步骤

1. **备份数据**: 备份PostgreSQL和Neo4j数据
2. **迁移节点**: 创建Neo4j节点
3. **迁移关系**: 创建Neo4j关系
4. **更新关联**: 更新PostgreSQL的`neo4j_node_id`
5. **验证一致性**: 检查数据一致性

---

## 6. 性能优化

### 6.1 PostgreSQL优化

- 为`neo4j_node_id`字段创建索引
- 为常用查询字段创建复合索引
- 使用JSONB字段存储扩展元数据

### 6.2 Neo4j优化

- 为节点属性创建索引
- 为关系类型创建索引
- 使用约束确保数据完整性

### 6.3 查询优化

- 使用Neo4j进行关系查询
- 使用PostgreSQL进行聚合统计
- 缓存常用查询结果

---

## 7. 监控与维护

### 7.1 数据一致性监控

- 定期检查PostgreSQL和Neo4j的数据一致性
- 监控同步失败的情况
- 自动修复不一致数据

### 7.2 性能监控

- 监控PostgreSQL查询性能
- 监控Neo4j查询性能
- 监控同步服务性能

---

## 8. 实施时间表

| 阶段 | 任务 | 预计时间 | 状态 |
|-----|------|---------|------|
| 第一阶段 | 数据模型设计 | 1天 | ✅ 已完成 |
| 第二阶段 | 数据同步服务 | 3天 | 🔄 进行中 |
| 第三阶段 | 数据迁移 | 2天 | ⏳ 待开始 |
| 第四阶段 | 查询优化 | 2天 | ⏳ 待开始 |

---

## 9. 风险评估

### 9.1 技术风险

**风险**: 数据同步失败导致不一致
**对策**: 实现事务回滚和补偿机制

**风险**: Neo4j性能问题
**对策**: 优化Cypher查询，使用索引

### 9.2 业务风险

**风险**: 迁移过程中服务中断
**对策**: 采用灰度迁移，逐步切换

---

## 10. 总结

通过将企业架构数据分离存储到不同的数据库：
- ✅ **PostgreSQL**: 存储实体元数据，支持事务和复杂查询
- ✅ **Neo4j**: 存储关系数据，支持图遍历和路径查询
- ✅ **Qdrant**: 存储向量数据，支持语义搜索（可选）

这种设计能够充分发挥各数据库的优势，实现最优的性能和可扩展性。

