# 统一意图识别MVP测试验证指南

## 📋 前置要求

1. **Docker和Docker Compose已安装**
2. **Python 3.8+**（用于运行测试脚本）
3. **requests库**：`pip install requests`

## 🚀 启动服务

### Windows (PowerShell)

```powershell
# 启动测试所需服务
.\start_services_for_test.ps1

# 或者手动启动
docker-compose up -d postgres redis agent-service
```

### Linux/Mac

```bash
# 启动测试所需服务
chmod +x start_services_for_test.sh
./start_services_for_test.sh

# 或者手动启动
docker-compose up -d postgres redis agent-service
```

### 检查服务状态

```bash
# 查看服务状态
docker-compose ps agent-service

# 查看服务日志
docker-compose logs -f agent-service

# 检查健康状态
curl http://localhost:8010/api/v1/health
```

## 🧪 运行测试

### 方式1：使用Python测试脚本（推荐）

```bash
# 安装依赖
pip install requests

# 运行测试
python test_unified_intent_mvp.py
```

### 方式2：使用curl测试

```bash
# 测试GET方式（SSE流式响应）
curl -N "http://localhost:8010/api/v1/unified/process?input=你好&user_id=test001&session_id=session001"

# 测试POST方式
curl -X POST "http://localhost:8010/api/v1/unified/process" \
  -H "Content-Type: application/json" \
  -d '{
    "user_input": "查询采购订单",
    "user_id": "test001",
    "session_id": "session001"
  }'
```

### 方式3：使用浏览器测试

1. 打开浏览器访问：`http://localhost:8010/docs`
2. 找到 `/api/v1/unified/process` 端点
3. 点击 "Try it out"
4. 输入测试数据并执行

## 📊 预期结果

### 成功响应示例

```
data: {"stage":"received","status":"success","progress":5,"message":"请求已接收，正在处理...","timestamp":"2024-01-01T12:00:00"}

data: {"stage":"processing","status":"analyzing","progress":20,"message":"正在分析用户意图...","timestamp":"2024-01-01T12:00:01"}

data: {"stage":"intent_complete","status":"complete","progress":50,"result":{"task_type":"simple_query","confidence":0.95},"timestamp":"2024-01-01T12:00:02"}

data: {"stage":"execution","status":"executing","progress":60,"message":"正在执行任务...","timestamp":"2024-01-01T12:00:03"}

data: {"stage":"complete","status":"success","progress":100,"result":{...},"timestamp":"2024-01-01T12:00:05"}
```

### 处理阶段说明

1. **received** (5%)：请求已接收
2. **processing** (20%)：开始处理
3. **intent_complete** (50%)：意图分析完成
4. **execution** (60%)：开始执行
5. **complete** (100%)：执行完成

## 🔍 故障排查

### 问题1：服务无法启动

```bash
# 检查Docker是否运行
docker info

# 检查端口是否被占用
netstat -ano | findstr :8010  # Windows
lsof -i :8010  # Linux/Mac

# 查看详细日志
docker-compose logs agent-service
```

### 问题2：无法连接到服务

```bash
# 检查服务是否在运行
docker-compose ps agent-service

# 检查服务健康状态
curl http://localhost:8010/api/v1/health

# 检查网络连接
ping localhost
```

### 问题3：SSE流式响应不工作

```bash
# 检查响应头
curl -I "http://localhost:8010/api/v1/unified/process?input=test"

# 应该看到：
# Content-Type: text/event-stream
# Cache-Control: no-cache
# Connection: keep-alive
```

### 问题4：导入错误

如果遇到模块导入错误，检查：
1. `services/unified_intent_mvp.py` 文件是否存在
2. `agent-service/src/routes/unified_intent_mvp.py` 文件是否存在
3. 服务是否已重新加载（Docker会自动热重载）

## 📝 测试检查清单

- [ ] 服务成功启动
- [ ] 健康检查通过
- [ ] GET方式SSE流式响应正常
- [ ] POST方式SSE流式响应正常
- [ ] 进度更新正常（5% → 20% → 50% → 60% → 100%）
- [ ] 意图分析结果正确
- [ ] 执行结果正确
- [ ] 错误处理正常

## 🎯 下一步

测试通过后，可以：
1. 集成到前端（使用EventSource）
2. 添加更多测试用例
3. 性能测试和优化
4. 进入阶段2：组件流式化




