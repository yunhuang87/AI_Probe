# 项目管理数据权限实现检查报告

## 检查时间
2025-12-24

## 一、权限控制实现情况

### 1. 后端权限过滤 ✅ 已实现

#### 1.1 项目列表API权限过滤

**文件**: `project-management/src/routes/projects.py`

**函数**: `list_projects` (第128-246行)

**实现逻辑**:
```python
# 获取用户有权限的项目ID列表
user_project_ids = get_user_projects(user_id, db, is_admin=is_admin)

# 如果是管理员，返回所有项目
if user_project_ids is None:
    query = db.query(Project)
# 如果没有权限的项目，返回空列表
elif not user_project_ids:
    return ProjectListResponse(total=0, items=[])
else:
    # 只返回用户有权限的项目
    query = db.query(Project).filter(Project.id.in_(user_project_ids))
```

**权限判断**:
- ✅ 管理员可以看到所有项目
- ✅ 普通用户只能看到有权限的项目

---

#### 1.2 用户项目权限获取函数

**文件**: `project-management/src/middleware/project_permissions.py`

**函数**: `get_user_projects` (第156-196行)

**实现逻辑**:
```python
def get_user_projects(
    user_id: UUID,
    db: Session,
    role: Optional[ProjectMemberRole] = None,
    is_admin: bool = False
) -> Optional[list]:
    # 如果是管理员，返回None表示所有项目
    if is_admin:
        return None
    
    # 查询用户在ProjectMember表中的项目
    query = db.query(ProjectMember.project_id).filter(
        ProjectMember.user_id == user_id
    )
    
    # 也包含用户作为manager的项目
    project_ids = db.query(Project.id).filter(Project.manager_id == user_id).all()
    member_project_ids = query.all()
    
    # 合并两个来源的项目ID
    all_project_ids = set()
    for pid in project_ids:
        all_project_ids.add(pid[0])
    for pid in member_project_ids:
        all_project_ids.add(pid[0])
    
    return list(all_project_ids)
```

**权限来源**:
1. ✅ **项目经理权限**: 用户作为 `Project.manager_id` 的项目（第187行）
2. ✅ **项目成员权限**: 用户在 `ProjectMember` 表中的项目（第178-180行）

---

### 2. 项目详情API权限控制 ✅ 已实现

**文件**: `project-management/src/routes/projects.py`

**函数**: `get_project` (第250-318行)

**实现逻辑**:
```python
@router.get("/{project_id}", response_model=ProjectResponse)
async def get_project(
    project_id: str, 
    db: Session = Depends(get_db),
    current_user: dict = Depends(require_project_permission(ProjectPermission.READ))
):
    """获取项目详情（需要项目查看权限）"""
```

**权限检查**:
- ✅ 使用 `require_project_permission(ProjectPermission.READ)` 依赖注入
- ✅ 自动检查用户是否有项目查看权限
- ✅ 如果没有权限，返回 403 Forbidden

---

### 3. 权限检查中间件 ✅ 已实现

**文件**: `project-management/src/middleware/project_permissions.py`

**函数**: `check_project_permission` (第26-80行)

**权限判断规则**:
1. ✅ **管理员**: 有所有权限
2. ✅ **项目经理** (`Project.manager_id == user_id`): 有所有权限
3. ✅ **项目成员** (`ProjectMember.role == "manager"`): 有所有权限
4. ✅ **项目成员** (`ProjectMember.role == "member"`): 可以创建、查看、更新（不能删除）
5. ✅ **查看者** (`ProjectMember.role == "viewer"`): 只能查看

---

## 二、具体场景验证

### 场景：彭静是项目经理

**需求**: 彭静只能看到项目经理是彭静的项目

**实现验证**:

1. **项目列表过滤**:
   - `get_user_projects` 函数会查询 `Project.manager_id == 彭静的用户ID` 的项目
   - 这些项目会被包含在返回的项目ID列表中
   - `list_projects` API 会过滤，只返回这些项目

2. **项目详情访问**:
   - 如果彭静尝试访问其他项目经理的项目
   - `check_project_permission` 会检查 `project.manager_id == 彭静的用户ID`
   - 如果不匹配，且不在 `ProjectMember` 表中，会返回 403 Forbidden

3. **权限来源**:
   - ✅ 主要来源: `Project.manager_id == user_id`
   - ✅ 补充来源: `ProjectMember` 表中的记录

---

## 三、潜在问题和建议

### 1. 权限来源优先级

**当前实现**: 
- `Project.manager_id` 和 `ProjectMember` 表中的项目都会被包含

**建议**:
- ✅ 当前实现合理，两个来源都有效

### 2. 数据一致性

**潜在问题**:
- 如果项目的 `manager_id` 被修改，但 `ProjectMember` 表中没有相应更新，可能导致权限不一致

**建议**:
- 在更新 `manager_id` 时，同步更新 `ProjectMember` 表
- 或者在 `get_user_projects` 中只使用 `Project.manager_id`（如果不需要 `ProjectMember` 表的额外权限）

### 3. 性能优化

**当前实现**:
- 查询两次数据库（`Project.manager_id` 和 `ProjectMember`）

**建议**:
- 可以考虑使用 JOIN 查询优化性能

---

## 四、测试建议

### 1. 功能测试

1. **测试场景1**: 彭静登录，查看项目列表
   - 预期: 只显示 `manager_id` 是彭静的项目
   - 验证: 检查返回的项目列表

2. **测试场景2**: 彭静尝试访问其他项目经理的项目详情
   - 预期: 返回 403 Forbidden
   - 验证: 检查 API 响应

3. **测试场景3**: 管理员登录，查看项目列表
   - 预期: 显示所有项目
   - 验证: 检查返回的项目列表

### 2. 边界测试

1. **测试场景4**: 用户没有任何项目权限
   - 预期: 返回空列表
   - 验证: 检查返回的项目列表

2. **测试场景5**: 项目没有设置 `manager_id`
   - 预期: 只有 `ProjectMember` 表中的用户可以看到
   - 验证: 检查权限判断逻辑

---

## 五、总结

### ✅ 已实现的功能

1. ✅ 项目列表API权限过滤
2. ✅ 项目详情API权限控制
3. ✅ 项目经理权限判断（`Project.manager_id`）
4. ✅ 项目成员权限判断（`ProjectMember` 表）
5. ✅ 管理员权限（可以看到所有项目）

### ⚠️ 需要注意的点

1. ⚠️ 确保 `Project.manager_id` 和 `ProjectMember` 表的数据一致性
2. ⚠️ 考虑性能优化（使用 JOIN 查询）

### 📝 建议

1. 添加单元测试验证权限逻辑
2. 添加集成测试验证端到端权限控制
3. 考虑添加权限审计日志

---

## 六、验证命令

### 检查数据库中的项目权限数据

```sql
-- 查看所有项目的项目经理
SELECT id, name, manager_id 
FROM pm_projects 
WHERE manager_id IS NOT NULL;

-- 查看项目成员表
SELECT project_id, user_id, role 
FROM pm_project_members;

-- 查看特定用户（彭静）作为项目经理的项目
SELECT p.id, p.name, p.manager_id, u.username, u.full_name
FROM pm_projects p
JOIN users u ON p.manager_id = u.id
WHERE u.username = '彭静' OR u.full_name = '彭静';
```

---

## 结论

**数据权限控制已经实现** ✅

- 项目经理（如彭静）只能看到 `manager_id` 是自己的项目
- 权限过滤在 API 层面实现，确保数据安全
- 支持管理员、项目经理、项目成员、查看者等多种角色

**建议进行实际测试验证**，确保在真实环境中正常工作。

