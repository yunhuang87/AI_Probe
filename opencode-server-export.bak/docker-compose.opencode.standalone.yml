# OpenCode 独立部署 - 不依赖已有网络（自行创建 enterprise-ai-network）
# 当报错 "network enterprise-ai-network declared as external, but could not be found" 时使用本文件
# 启动: docker compose -f docker-compose.opencode.standalone.yml --env-file .env up -d

services:
  opencode:
    image: enterprise-ai-opencode:latest
    container_name: enterprise-ai-opencode
    working_dir: /workspace
    command: opencode serve --hostname 0.0.0.0 --port 4096
    ports:
      - "${OPENCODE_PORT:-4096}:4096"
    environment:
      - OPENCODE_SERVER_USERNAME=${OPENCODE_SERVER_USERNAME:-opencode}
      - OPENCODE_SERVER_PASSWORD=${OPENCODE_SERVER_PASSWORD:-}
    volumes:
      - ${OPENCODE_WORKSPACE:-.}:/workspace:cached
    networks:
      - enterprise-ai-network
    restart: unless-stopped

networks:
  enterprise-ai-network:
    driver: bridge
